---
phase: 04-devices2-wire-shape-migration-v2-0
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - usbee@bitcreed.us/metadata.json
  - CHANGELOG.md
  - usbee@bitcreed.us/po/usbee@bitcreed.us.pot
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "metadata.json version-name is '2.0.0' and version integer is incremented by 1 from v1.2.0"
    - "CHANGELOG.md carries a ## [2.0.0] entry that names MIN_USBEEHIVE_VERSION explicitly and lists the regex deletions"
    - "po/usbee@bitcreed.us.pot is regenerated and contains the new label-table + empty-state + UX-1/UX-2 strings"
    - "MIN_USBEEHIVE_VERSION value is re-verified against the actual usbeehive tag at release time"
    - "gnome-extensions pack produces a clean zip; gnome-extensions install --force on the dev machine succeeds"
    - "Annotated tag v2.0.0 exists locally with message body sourced from the CHANGELOG entry"
    - "EGO upload steps are documented for the user; tag push happens at a human checkpoint"
  artifacts:
    - path: "usbee@bitcreed.us/metadata.json"
      provides: "v2.0.0 release manifest"
      contains: "version-name 2.0.0"
    - path: "CHANGELOG.md"
      provides: "## [2.0.0] release notes"
      contains: "MIN_USBEEHIVE_VERSION"
    - path: "usbee@bitcreed.us/po/usbee@bitcreed.us.pot"
      provides: "Regenerated gettext .pot with new strings"
      contains: "Serial"
  key_links:
    - from: "git tag v2.0.0"
      to: ".github/workflows/release.yml"
      via: "tag push triggers zip build + GitHub release"
      pattern: "vX.Y.Z"
    - from: "GitHub release zip"
      to: "https://extensions.gnome.org/upload/"
      via: "manual EGO upload"
---

<objective>
Cut the v2.0.0 release. Bump `metadata.json`, write the `## [2.0.0]` CHANGELOG entry that names the minimum usbeehive version, regenerate the gettext `.pot` so the new label-table / driver-flag / subclass / out-of-date strings enter the translation catalog, build the zip locally via `gnome-extensions pack`, verify the zip installs and enables cleanly, create the annotated tag, and document EGO upload steps for the user.

Purpose: This plan is the release plumbing. It contains no behaviour changes — Plan 04-02 finished the migration. What it does include is **two human checkpoints**: (a) confirm the `MIN_USBEEHIVE_VERSION` constant matches the actual upstream usbeehive tag (the placeholder `"0.6.0"` may need bumping if the daemon shipped under a different version), and (b) gate the `git push origin v2.0.0` behind explicit user approval, because pushing the tag triggers the release.yml CI workflow and the EGO upload follows. The user wants final eyes on the tag before push.

Output: A locally-tagged `v2.0.0`, an EGO-ready zip, and a documented upload runbook. The actual `git push origin v2.0.0` and the EGO upload happen post-checkpoint, by the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-devices2-wire-shape-migration-v2-0/04-CONTEXT.md
@.planning/phases/04-devices2-wire-shape-migration-v2-0/04-01-SUMMARY.md
@.planning/phases/04-devices2-wire-shape-migration-v2-0/04-02-SUMMARY.md
@usbee@bitcreed.us/metadata.json
@CHANGELOG.md

<release_process>
<!-- From CLAUDE.md section "Release Process" — single source of truth for the steps. -->

Version fields in metadata.json:
- version (EGO-required monotonic integer; +1 per EGO submission).
- version-name (human-facing semver; bumped per release).

Steps from CLAUDE.md:
1. Bump metadata.json (version-name to 2.0.0; version to previous + 1).
2. Update CHANGELOG.md with a new "## [X.Y.Z]" section and "[X.Y.Z]:" ref link.
3. Commit version bump + changelog.
4. Tag with annotated tag — tag name MUST match version-name.
5. Push tag — release.yml extracts CHANGELOG section, builds zip, creates GitHub Release.
6. Upload zip to EGO at https://extensions.gnome.org/upload/.

Current metadata.json pre-bump:
  "version": 1
  "version-name": "1.2.0"

Post-bump targets:
  "version": 2
  "version-name": "2.0.0"
</release_process>

<post_release_followup>
<!-- From STATE.md lines 107-113 — re-mentioned in Task 8 so the user is reminded
     once EGO assigns the extension ID. -->

After uploading to https://extensions.gnome.org/upload/ and receiving the assigned
extension ID, the user runs (cosmetic only, does not block EGO submission):
  gh repo edit abrauchli/usbee --homepage "https://extensions.gnome.org/extension/ID/slug/"
</post_release_followup>

</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm MIN_USBEEHIVE_VERSION value against the actual usbeehive tag</name>
  <decision>
    Plan 04-02 wired MIN_USBEEHIVE_VERSION = '0.6.0' as a placeholder per CONTEXT D-2.0-07. Before tagging USBee v2.0.0, confirm this value matches the version of usbeehive that actually ships Devices2.
  </decision>
  <context>
    The daemon's Cargo.toml was still 0.5.1 at the time of Phase 04 planning (CONTEXT.md risk register: "MIN_USBEEHIVE_VERSION ships pointing at a daemon version that was never tagged"). The daemon CHANGELOG section was [Unreleased] at the time of writing.

    Pre-decision verification — run before opening the options below:

      cd ../usbeehive
      git tag --list 'v*' --sort=-version:refname | head -3
      grep '^version' Cargo.toml
      git log --oneline -1 -- src/dbus.rs

    Cross-check that the latest tag has Devices2:
      cd ../usbeehive
      git show v_LATEST_:src/dbus.rs | grep -E "Devices1|Devices2|interface.name"
  </context>
  <options>
    <option id="option-a">
      <name>Keep 0.6.0 as-is</name>
      <pros>Matches the locked CONTEXT D-2.0-07 placeholder; no further changes to dbus-client.js needed.</pros>
      <cons>Only correct if usbeehive 0.6.0 has actually been tagged with Devices2. If the daemon shipped under a different version (e.g. 1.0.0), the gate is misaligned and users on the actual ship version will be rejected as out-of-date.</cons>
    </option>
    <option id="option-b">
      <name>Bump constant to the actual tagged value</name>
      <pros>Gate is correct against the actual ship version. No user-side rejection of valid daemons.</pros>
      <cons>Requires a small follow-up edit in src/dbus-client.js (one-line change to MIN_USBEEHIVE_VERSION) plus a CHANGELOG note that the constant was re-pinned at release time. Adds a sixth commit to this plan.</cons>
    </option>
    <option id="option-c">
      <name>Daemon has not yet shipped Devices2 publicly — hold v2.0.0</name>
      <pros>Lockstep release intact (D-2.0-09); we don't ship USBee v2.0.0 referencing a daemon version that doesn't exist yet.</pros>
      <cons>Phase 04 stays open; the build artifact is held until daemon ships.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b (provide corrected version), or option-c.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bump metadata.json to v2.0.0</name>
  <files>usbee@bitcreed.us/metadata.json</files>
  <action>
    Edit usbee@bitcreed.us/metadata.json:

    - version-name: "1.2.0" → "2.0.0" (REL-01).
    - version: 1 → 2 (EGO monotonic integer increment per CLAUDE.md Release Process step 1).
    - description: update the one-line summary describing which D-Bus interface USBee consumes — replace "org.usbeehive.Devices1" with "org.usbeehive.Devices2". Everything else in the description stays.
    - shell-version array: unchanged (still ["46", "47", "48", "49", "50"]); no GNOME Shell API surface changed.

    Do NOT touch uuid (usbee@bitcreed.us), url, gettext-domain, or settings-schema. Those are EGO-stable identifiers.

    Commit message: chore: release v2.0.0 (metadata bump)
  </action>
  <verify>
    <automated>grep -q "\"version-name\": \"2.0.0\"" usbee@bitcreed.us/metadata.json &amp;&amp; grep -q "\"version\": 2" usbee@bitcreed.us/metadata.json &amp;&amp; grep -q "org.usbeehive.Devices2" usbee@bitcreed.us/metadata.json &amp;&amp; ! grep -q "Devices1" usbee@bitcreed.us/metadata.json</automated>
  </verify>
  <done>metadata.json declares version-name 2.0.0, version 2, references Devices2 only. JSON parses (python3 -m json.tool exits 0).</done>
</task>

<task type="auto">
  <name>Task 3: Write the ## [2.0.0] CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <action>
    Edit CHANGELOG.md to insert a new "## [2.0.0]" section immediately after the existing header block and before the existing "## [1.2.0]" entry. Date the entry with today's date (the executor's local date) in ISO-8601 form.

    The entry MUST include the following sub-sections (REL-02):

    1. A 2-3 sentence prose preamble naming the breaking daemon dependency explicitly: "Requires usbeehive >= 0.6.0 (or the value re-pinned in Task 1). USBee refuses to consume any older daemon, surfacing a dedicated 'Daemon out of date' empty state instead. MIN_USBEEHIVE_VERSION is declared in src/dbus-client.js and is the single source of truth for the gate."

    2. ### Changed (breaking — D-Bus interface)
       - Migrated to org.usbeehive.Devices2 (was Devices1). 19-field structured DeviceEntry replaces the v1 prose-bullets shape. Diagnose(port) return type is now (bsssb) with a leading present bool.
       - Daemon-version gate: USBee reads the Version D-Bus property on proxy-ready and refuses daemons older than MIN_USBEEHIVE_VERSION (currently 0.6.0).

    3. ### Added
       - src/label-table.js — machine-key to gettext display-label resolver covering serial, mount, drivers, data_role, power_mode, pd_revision, plug_orientation, pd_contract, cable_speed, cable_current, cable_max_power, cable_type, cable_vendor, charger_max, usb_power_ma. Unknown keys render the raw key (forward-compatible per D-2.0-06).
       - device_class enum lookup table for symbolic icons (19 daemon variants). Daemon-supplied icon field is preferred when it passes the SYMBOLIC_ICON_RE guard.
       - Status::Sourcing surfaced in the tile subtitle as "Powering: N W out". Sourcing entries do not trigger the issue-first sort.
       - primary_driver == "" flag: an italic "Driver: not bound" row appears at the top of the detail panel for devices the daemon could not bind a driver to.
       - device_subclass detail-panel row when the daemon emits a non-empty subclass string.
       - New empty-state variant: "Daemon out of date — please update usbeehive" with copy distinct from the existing "Daemon not running".
       - Forward-compatibility regression test (src/forward-compat.test.js) asserting unknown enum values fall through to safe defaults.

    4. ### Removed (regex layer)
       Deleted from src/device-store.js:
         WATT_RE, DIRECTION_RE, USB_VERSION_RE (incl. WR-03 lookahead patch), SPEED_RE,
         parseWatts, parseDirection, parseLinkSpeed, DIAG_PHRASES.
       Deleted from src/popover.js:
         keyForBullet (regex/keyword label-inference helper).
       Deleted from src/device-icon.js:
         KEYWORD_MAP (driver/headline keyword table) and the headline-scan resolution path.
       Preserved (intentional):
         SYMBOLIC_ICON_RE in src/device-icon.js (security mitigation T-03-01).
         formatWatts in src/device-store.js (UI formatting helper).

    5. ### Notes
       - First EGO submission. v1.0, v1.1, and v1.2 were built but never uploaded to extensions.gnome.org per CONTEXT D-2.0-08.
       - shell-version array unchanged at ["46", "47", "48", "49", "50"]; no GNOME Shell API surface change.

    Append at the bottom of the file: [2.0.0]: https://github.com/abrauchli/usbee/releases/tag/v2.0.0

    Commit message: docs: ## [2.0.0] CHANGELOG entry for Devices2 migration
  </action>
  <verify>
    <automated>grep -q "^## \[2.0.0\]" CHANGELOG.md &amp;&amp; grep -q "MIN_USBEEHIVE_VERSION" CHANGELOG.md &amp;&amp; grep -q "org.usbeehive.Devices2" CHANGELOG.md &amp;&amp; grep -q "WATT_RE" CHANGELOG.md &amp;&amp; grep -q "KEYWORD_MAP" CHANGELOG.md &amp;&amp; grep -q "keyForBullet" CHANGELOG.md &amp;&amp; grep -q "\[2.0.0\]: https://github.com/abrauchli/usbee/releases/tag/v2.0.0" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md has a complete ## [2.0.0] section naming MIN_USBEEHIVE_VERSION, listing every deleted regex symbol, and explaining the new label-table / Sourcing / driver-flag / subclass / out-of-date features. Reference link present at file bottom.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate the gettext .pot template</name>
  <files>usbee@bitcreed.us/po/usbee@bitcreed.us.pot</files>
  <action>
    Regenerate the .pot file via xgettext over every JS source file in the extension (PACK-02 invariant from Phase 02; equivalent re-extraction needed because Plan 04-02 introduced new translation candidates).

    Run from the project root:

      xgettext --from-code=UTF-8 \
               --keyword=_ --keyword=N_ \
               --add-comments=TRANSLATORS \
               --package-name="USBee" \
               --package-version="2.0.0" \
               -o usbee@bitcreed.us/po/usbee@bitcreed.us.pot \
               usbee@bitcreed.us/src/*.js \
               usbee@bitcreed.us/extension.js \
               usbee@bitcreed.us/prefs.js

    Verify the new strings landed:

      grep "msgid \"Serial\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot
      grep "msgid \"Cable speed\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot
      grep "msgid \"Subclass\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot
      grep "msgid \"Driver\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot
      grep "msgid \"not bound\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot
      grep "msgid \"usbeehive daemon out of date\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot

    All six greps must hit. If any is missing, the source file containing that string was not in the xgettext input set, or the gettext marker is malformed — fix at source and re-run.

    Do NOT manually edit the .pot file. The header may declare CHARSET=UTF-8 in the placeholder line; that is xgettext's default and is fine — translators replace it when they fill in a .po.

    Commit message: chore(i18n): regenerate .pot for v2.0.0 (label-table + UX-1/UX-2 + out-of-date strings)
  </action>
  <verify>
    <automated>test -f usbee@bitcreed.us/po/usbee@bitcreed.us.pot &amp;&amp; grep -q "msgid \"Serial\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot &amp;&amp; grep -q "msgid \"Cable speed\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot &amp;&amp; grep -q "msgid \"Subclass\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot &amp;&amp; grep -q "msgid \"not bound\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot &amp;&amp; grep -q "msgid \"usbeehive daemon out of date\"" usbee@bitcreed.us/po/usbee@bitcreed.us.pot</automated>
  </verify>
  <done>.pot file exists, contains every new translation candidate string from Plan 04-02 (Serial, Cable speed, Subclass, Driver, not bound, usbeehive daemon out of date, etc.). Committed.</done>
</task>

<task type="auto">
  <name>Task 5: Build and smoke-test the v2.0.0 zip</name>
  <files></files>
  <action>
    Build the EGO submission zip via gnome-extensions pack and verify it installs cleanly on the dev machine. From the project root:

      cd usbee@bitcreed.us
      gnome-extensions pack \
        --podir=po \
        --schema=schemas/us.bitcreed.usbee.gschema.xml \
        --extra-source=src \
        --extra-source=stylesheet.css \
        --extra-source=dbus-iface.xml \
        --extra-source=icons \
        --force

    Expected output: usbee@bitcreed.us.shell-extension.zip in the current directory (the in-tree usbee@bitcreed.us/ directory). The zip is NOT committed to the repo (CLAUDE.md "What does not get published" section: ".gitignore excludes it"). Confirm:

      git status usbee@bitcreed.us.shell-extension.zip
      # Expected: "Untracked files" — and gitignored.

    Sanity-check zip contents:

      unzip -l usbee@bitcreed.us.shell-extension.zip | head -40

    The zip must contain: metadata.json (declaring version-name 2.0.0), extension.js, prefs.js, dbus-iface.xml (the Devices2 version), src/*.js (including label-table.js, forward-compat.test.js is optional but allowed), schemas/gschemas.compiled (auto-compiled by pack), po/*.mo if any locales exist (none for v2.0 per CONTEXT — pot only), and the icons directory. Must NOT contain any .py, .so, binary, .planning/, or the zip itself (no self-inclusion).

    Smoke-test install:

      gnome-extensions install --force usbee@bitcreed.us.shell-extension.zip
      # On Xorg: Alt+F2 → r to reload the shell.
      # On Wayland: full re-login required.
      gnome-extensions enable usbee@bitcreed.us
      gnome-extensions info usbee@bitcreed.us | head -10

    Expected: gnome-extensions info reports state ENABLED, version 2.0.0 (or 2 per the integer field), no errors in journalctl --user-unit gnome-shell-x11 -f from the enable step.

    Open the tile, confirm devices render via the Devices2 wire (same UX validated in Plan 04-02 Task 14 smoke test). No new functional changes since 04-02 — this is purely re-running the same checks against the zip-installed version rather than the in-tree directory.

    No commit (the zip is not tracked).
  </action>
  <verify>
    <automated>test -f usbee@bitcreed.us/usbee@bitcreed.us.shell-extension.zip &amp;&amp; unzip -l usbee@bitcreed.us/usbee@bitcreed.us.shell-extension.zip | grep -q metadata.json &amp;&amp; unzip -l usbee@bitcreed.us/usbee@bitcreed.us.shell-extension.zip | grep -q "src/label-table.js" &amp;&amp; unzip -l usbee@bitcreed.us/usbee@bitcreed.us.shell-extension.zip | grep -q "dbus-iface.xml" &amp;&amp; ! unzip -l usbee@bitcreed.us/usbee@bitcreed.us.shell-extension.zip | grep -qE "\\.so|\\.bin|\\.planning"</automated>
  </verify>
  <done>Zip built, contains label-table.js + dbus-iface.xml (Devices2) + metadata.json (version 2.0.0), no binaries / no .planning/. gnome-extensions install + enable succeeded; tile renders without errors in journalctl.</done>
</task>

<task type="auto">
  <name>Task 6: Create the annotated v2.0.0 tag locally</name>
  <files></files>
  <action>
    Create the annotated tag whose message body is the CHANGELOG ## [2.0.0] section. Per CLAUDE.md §Release Process step 4: "tag name MUST match version-name exactly — the release workflow asserts this and fails otherwise."

    Extract the changelog section into a tag message:

      cd /home/blk/projects/rust/usbee
      TAG_MSG=$(awk '/^## \[2.0.0\]/{flag=1; next} /^## \[/{flag=0} flag' CHANGELOG.md)
      git tag -a v2.0.0 -m "v2.0.0" -m "$TAG_MSG"

    Verify the tag exists locally:

      git tag --list 'v2.0.0'
      git show v2.0.0 --no-patch | head -20

    Expected: tag is annotated (not lightweight); message body contains the "Requires usbeehive >= 0.6.0" preamble and the section sub-headers from Task 3.

    **CRITICAL: Do NOT push the tag.** That is Task 7's human checkpoint.

    No new commit — the tag is itself the artifact.
  </action>
  <verify>
    <automated>git tag --list 'v2.0.0' | grep -q '^v2.0.0$' &amp;&amp; git for-each-ref refs/tags/v2.0.0 --format='%(objecttype)' | grep -q tag &amp;&amp; git show v2.0.0 --no-patch | grep -q "MIN_USBEEHIVE_VERSION"</automated>
  </verify>
  <done>Annotated tag v2.0.0 exists locally with the CHANGELOG body as message. Tag is NOT pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: User-gated tag push</name>
  <what-built>
    All release artifacts staged: metadata.json bumped, CHANGELOG entry written, .pot regenerated, zip built and smoke-tested, annotated tag v2.0.0 created locally. The only remaining step is `git push origin v2.0.0`, which triggers `.github/workflows/release.yml` to build the canonical zip and create the GitHub Release.
  </what-built>
  <how-to-verify>
    Before pushing, verify locally:

      git tag --list 'v2.0.0'
      git show v2.0.0 --no-patch | head -30
      git log --oneline -10
      git status

    Confirm:
    1. The tag commit matches the latest commit on master (or whichever branch the v2.0.0 work landed on).
    2. The tag message body is correct (CHANGELOG ## [2.0.0] section, including the MIN_USBEEHIVE_VERSION line).
    3. metadata.json version-name matches the tag name exactly ("2.0.0"). The release workflow asserts this and will fail otherwise.
    4. The build artifact zip is NOT in the git index (gitignored per CLAUDE.md §"What does not get published").

    When ready, the user runs:

      git push origin master
      git push origin v2.0.0

    The push of v2.0.0 triggers .github/workflows/release.yml. The workflow:
    - Verifies metadata.json version-name matches the tag.
    - Runs gnome-extensions pack to build the zip.
    - Extracts the matching CHANGELOG.md section as release notes.
    - Creates the GitHub Release and attaches the zip.

    After the workflow completes, verify at:
      https://github.com/abrauchli/usbee/releases/tag/v2.0.0
    The release page should show the zip attachment and the CHANGELOG body as the release notes.

    Report findings: pushed and workflow green / pushed and workflow failed (paste workflow logs) / not yet pushed (hold).
  </how-to-verify>
  <resume-signal>Type "pushed" once tag is up and the release workflow is green. Or "hold" with rationale, or paste failure logs.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: Upload zip to extensions.gnome.org and record the assigned extension ID</name>
  <what-built>
    The GitHub Release at https://github.com/abrauchli/usbee/releases/tag/v2.0.0 carries the canonical usbee@bitcreed.us.shell-extension.zip. EGO submission is the next step. EGO review is manual and asynchronous — there is no CLI for this.
  </what-built>
  <how-to-verify>
    Manual upload steps (CLAUDE.md §Release Process step 6):

    1. Download the zip from the GitHub Release page:
       https://github.com/abrauchli/usbee/releases/tag/v2.0.0
       (Click the .shell-extension.zip asset.)

    2. Open https://extensions.gnome.org/upload/ in a browser.

    3. Sign in with the developer account that owns the EGO listing (per CONTEXT D-2.0-08, this is USBee's first EGO submission — the account is the one that will be assigned the extension ID).

    4. Upload the zip. EGO will:
       - Run automated validators (manifest checks, shell-version array, no binaries, no Gtk/Adw in Shell-process code, no synchronous D-Bus). The Phase 02 EGO audit gates 1-9 must remain green; they were last validated in v1.1 and the v2.0 cutover did not introduce new violations.
       - Queue for human review. Initial reviews typically land within days; the queue is volunteer-staffed.

    5. EGO assigns an extension ID and a slug (e.g. /extension/12345/usbee/). Record both — they are needed for the post-submission GitHub repo homepage URL update (see STATE.md lines 107-113 and the <post_release_followup> block in this plan's context).

    Post-submission (STATE.md follow-up — does NOT block the EGO upload itself):

      gh repo edit abrauchli/usbee --homepage "https://extensions.gnome.org/extension/_ID_/_slug_/"

    Until EGO assigns the ID, the repo's homepage field carries a placeholder.

    Report findings: uploaded and pending review (paste the EGO submission URL + assigned ID once available) / upload rejected (paste reviewer feedback).
  </how-to-verify>
  <resume-signal>Type "uploaded" with the EGO submission URL and assigned ID, or paste reviewer feedback if rejected.</resume-signal>
</task>

</tasks>

<verification>
- metadata.json declares version-name "2.0.0" and version 2; references org.usbeehive.Devices2.
- CHANGELOG.md ## [2.0.0] section names MIN_USBEEHIVE_VERSION, lists every deleted regex symbol, declares the new daemon dependency, and has a footer reference link to releases/tag/v2.0.0.
- usbee@bitcreed.us/po/usbee@bitcreed.us.pot contains msgid entries for Serial, Cable speed, Subclass, Driver, not bound, usbeehive daemon out of date.
- usbee@bitcreed.us/usbee@bitcreed.us.shell-extension.zip exists (gitignored), contains src/label-table.js and the Devices2 dbus-iface.xml, contains no .so / .bin / .planning entries.
- Annotated tag v2.0.0 exists locally; git show v2.0.0 shows the CHANGELOG body.
- Task 7 human checkpoint: tag pushed; release.yml workflow green; GitHub Release page has the zip.
- Task 8 human checkpoint: EGO upload submitted; extension ID recorded for STATE.md homepage follow-up.
</verification>

<success_criteria>
1. metadata.json: version-name 2.0.0, version 2 (REL-01).
2. CHANGELOG.md: ## [2.0.0] entry with MIN_USBEEHIVE_VERSION, regex-deletion list, and minimum daemon version statement (REL-02).
3. .pot regenerated with all new strings.
4. gnome-extensions pack produced a clean EGO-ready zip; install + enable succeeded.
5. Annotated tag v2.0.0 created locally with CHANGELOG-sourced message.
6. Tag pushed (Task 7 checkpoint approved); release.yml workflow green; GitHub Release page populated.
7. EGO upload submitted (Task 8 checkpoint); extension ID recorded for downstream GitHub homepage update (REL-03).
</success_criteria>

<output>
Create `.planning/phases/04-devices2-wire-shape-migration-v2-0/04-03-SUMMARY.md` when done. Include: commit SHAs for Tasks 2, 3, 4; tag SHA for Task 6; the GitHub Release URL once the workflow ran; the EGO submission URL + assigned extension ID once Task 8 completes; and a note that the STATE.md homepage-URL follow-up is still open (it remains pending until EGO assigns the ID and the user runs the gh repo edit command).
</output>
