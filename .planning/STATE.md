---
gsd_state_version: "1.0"
milestone: v2.0
current_phase: 04
current_phase_name: devices2-wire-shape-migration-v2-0
status: complete
stopped_at: Completed quick task 260910-r46 (v2.8.0 packed + installed locally; Shell restart outstanding)
last_updated: "2026-09-11T01:41:24.489Z"
last_activity: 2026-09-10
last_activity_desc: "Quick task 260910-p91: two independent changes. **(1)** The alt-mode `no_usb_pd` row now states a condition instead of issuing an instruction -- \"use a USB-C port and cable that support Power Delivery\" became **\"Alt mode did not start -- this connection does not provide USB Power Delivery, which needs both a port and a cable that support it\"**. This was the last remedy-shaped string in the UI, and `260910-o99` had flagged rather than swept it for a reason that still holds: here the daemon *names* the cause (`usb_altmode_state == Unsuccessful` with `no_usb_pd` in `usb_altmode_failure`), so unlike the three withdrawn claims the information is sound -- a PD link definitionally needs a capable port AND a capable cable. What was wrong was only the **mood**: this machine has no PD controller at all (`/sys/class/typec` absent), so the imperative resolved to \"buy a different motherboard\", telling the reader to do something they may have no path to. Restated, therefore, not deleted -- same gate, same amber treatment, same SVID naming, voice matched to the prior three (\"did not start\" mirrors the connector hint's \"did not come up\"). A full sweep of every `_()` in `src/` and `prefs.js` for imperative verbs returns exactly one hit and it is a noun (`_('Plug orientation')`), so this really was the last. Three comments asserting the remedy as fact were corrected so the belief is not re-seeded -- the same failure `260910-o99` fixed in four `notifier.js` comments. `deriveAltMode().actionable` kept (four assertions pin it) but redocumented as a row *selector*, not a claim a fix exists. The new CI guards deliberately inspect the extracted `_('...')` **literals** rather than the file text: the docstring and Translators comment quote the withdrawn imperative on purpose, and a substring test would have failed on the very comments that keep the decision from being forgotten. **(2)** CLAUDE.md's EGO framing corrected. USBee is **not published on extensions.gnome.org**, yet Release Process step 6 instructed the maintainer to download the zip and submit it at extensions.gnome.org/upload -- work that does not happen. Step 6 is gone; the release ends at the GitHub Release `release.yml` already creates (that workflow never mentioned EGO, which makes step 6 a pure documentation error, not a process one). The correction is narrow by design: the constraints EGO motivated -- no bundled binaries, no minified JS, documented API surface only -- are good on their own merits and cost nothing to keep, so only their *status* changed. A new **Distribution status** subsection states the position once and instructs the reader to read every other EGO mention through it; a new **Distribution** constraint bullet carries the same sentence; and a single preamble above \"What NOT to Use\" governs a dozen \"EGO reviewers will reject it\" cells rather than rewriting each one. Wording is **neutral on intent** -- the user has not said whether submission is abandoned or deferred, so every new sentence is true under both readings. The mirrored edits were made in `.planning/PROJECT.md` and `.planning/research/STACK.md` as well and verified byte-identical, because those CLAUDE.md sections are GSD-generated mirrors that the next docs-update would otherwise revert. README.md **did** misinstruct a user -- it headed the install section \"From extensions.gnome.org (recommended)\" and claimed a listing was pending review -- and was fixed; `ci.yml`'s \"EGO pre-check (shexli)\" job was left alone, since a lint that runs and passes is exactly the kept-by-choice posture. 391 assertions green (94/66/174/57) under `rtk proxy`; `.pot` delta verified rather than assumed at exactly -1/+1 with 153 unique msgids on both sides and the header intact; packed + installed (25 files / 240,775 B, 15 `src/` entries, `ls -ld` pre-flight confirmed a real directory). metadata.json untouched at 2.7.1 / 12. No version bump, no tag, no push, Shell not restarted"
state_head: 29a945bfb70b351241a589afef213d9b43133d89
progress:
  total_phases: 4
  completed_phases: 0
  total_plans: 3
  completed_plans: 3
milestone_name: Devices2 wire-shape migration
---

# State: USBee

**Last updated:** 2026-05-14 (milestone v2.0 opened — Phase 04 Devices2 migration roadmapped)

## Project Reference

- **Core value:** A GNOME-native, glanceable answer to "is this the fast port?" and "why is my laptop charging slowly?" — without opening a terminal.
- **Current focus:** Phase 04 — devices2-wire-shape-migration-v2-0
- **Mode:** mvp
- **Granularity:** coarse
- **Workflow mode:** yolo (parallelization enabled)

## Current Position

Phase: 04 (devices2-wire-shape-migration-v2-0) — COMPLETE
Plan: 3 of 3 (all done; EGO upload held for v2.1.0)
Status: **v2.8.0 cut locally but not released** -- `metadata.json` is at 13 / 2.8.0, the changelog section is dated 2026-09-10, and the zip is packed and installed on this machine, but there is **no `v2.8.0` tag and nothing is pushed** (`origin/master..HEAD` = 25 commits). The GitHub Release step is therefore still outstanding and is the user's to trigger. A GNOME Shell restart is also outstanding before the installed 2.8.0 actually loads. Last *published* release was v2.7.1 (run 34535975902, zip 24 entries / 204,819 B with `src/` intact); per 260910-p91 a release **ends at the GitHub Release** -- USBee is not submitted to extensions.gnome.org, so there is no EGO upload step pending. v2.8.0's content: the daemon-state probe, wrapping command row and prefs Start button (260910-myu), the Link/Capability row split (260910-n10), the withdrawal of the faster-port remedy (260910-o99) and the alt-mode PD restatement plus EGO-framing correction (260910-p91)
Last activity: 2026-09-15 -- Quick task 260915-ib9: removed the dead **`SnapshotJson`** declaration from USBee’s inlined `IFACE_XML` and from the reference copy `dbus-iface.xml`. Proved dead **before** removing, not after: a repo-wide `rtk proxy grep` found four mentions and a call-shaped grep found **none**. The two survivors are comments in `property-policy.js` describing the **daemon’s** method, left alone because they stay true -- usbeehive is untouched and still exports it, so `busctl` and the usbeehive CLI reach it exactly as before. The comment above `IFACE_XML` now states that relationship so those two do not later read as staleness, and records why **`Refresh` and `Diagnose` stay** though equally uncalled: both are declared against a planned call site, `SnapshotJson` was not. Two new tests pin their survival so this is not read as licence to strip them too. The **byte-equality invariant** (literal vs on-disk XML, less the doctype) was preserved and proved **twice** -- the suite’s own assertion, plus an independent normalisation outside it putting both sides at exactly **1464 bytes** with a clean `diff`. No daemon change, per the explicit scope limit. No Shell restart needed: this changes only the XML string a proxy is built from, with no UI surface. Committed straight to `master` with `git.allow_default_branch_commits` still unset -- inferred and flagged for audit, on the strength of every prior quick task doing the same
Previous activity: 2026-09-15 -- Quick task 260915-i4w: the filter switches now also live in the popover, under a collapsible **Options** submenu of `PopupSwitchMenuItem`s two-way bound to the same GSettings keys `prefs.js` binds, so either surface moves the other live. A fourth filter, **`hide-builtin-devices`** (`b`, default **false**), was added to both surfaces. It keys off the daemon’s own `mount == ‘fixed’` -- **verified at the usbeehive source** (`src/summary.rs:645` emits `mount` only as the literals `fixed`/`removable`, dropping `unknown` and the empty string per `src/usb.rs:71`) rather than inherited from the upstream project’s `removable == ‘fixed’` -- so an **absent** `mount` means "the kernel did not say" and is never treated as built-in. A test pins that, another pins the token’s case-sensitivity. Default `false` so nothing vanishes from an existing list on upgrade, and because a built-in device is a *real device* unlike a hub, which is why `show-hubs` defaults the other way. A built-in device **carrying an issue stays visible** -- the same escape hatch `show-hubs` uses (260905-b0s) -- so the header can never count an issue the list will not show. The predicate went into the **zero-import** `link-verdict.js` so bare-gjs CI can unit-test it rather than only guard it structurally. The two-way binding carries a **re-entrancy latch** instead of resting on `setToggleState()` not emitting `toggled`: Shell 50.1’s `ui/popupMenu.js` is **not extractable from any installed gresource** here, so that documented behaviour could not be confirmed locally and a write-loop between two bound surfaces was not left possible. Empty-after-filtering now says **"All devices hidden by the current filters"** instead of falsely claiming nothing is attached. 204 assertions green across all four suites under `rtk proxy`; schema round-tripped in an isolated `GSETTINGS_SCHEMA_DIR`. **Popover not verifiable without a Shell restart**; the prefs window was deliberately not exercised in-process, since the installed extension is a real directory holding the released v2.8.0, not a symlink to this checkout
Earlier activity: 2026-09-10 -- Quick task 260910-r46: cut **v2.8.0** and deployed it locally, stopping deliberately before tag and push. `metadata.json` went 12 -> **13** / 2.7.1 -> **2.8.0**; both edits are length-preserving, which is why the packed zip came out byte-identical in size to p91's. The `## [Unreleased]` section carried Added, Changed **and** Fixed subsections, so a **MINOR** bump was correct, and it rolled verbatim into `## [2.8.0] -- 2026-09-10` with a fresh empty `## [Unreleased]` above it and a `[2.8.0]:` link in the file's existing `releases/tag/vX.Y.Z` style. The file has **no `[Unreleased]:` compare link** and one was deliberately not invented. Changelog line count pinned at **680** as a deterministic guard -- a heading-only grep would miss a lost or duplicated section. The zip was **proved**, not assumed: **25 entries / 240,775 B with 15 `src/` entries**, the `src/` prefix being the real gate since a wrong `--extra-source` path exits 0 and prints nothing. Installed with `--force` only after an `ls -ld` pre-flight re-confirmed a real directory (the `260910-hrj` symlink hazard wipes the repo's extension sources otherwise); tree verified intact after. On-disk metadata read back under `rtk proxy` as `"version": 13` / `"version-name": "2.8.0"` -- deliberately **not** via `gnome-extensions info`, which reports stale in-process metadata until the Shell restarts. `git tag -l v2.8.0` empty, `origin/master..HEAD` 25 commits unpushed, both intended. **Shell restart outstanding** (Xorg: Alt+F2 -> r; Wayland: full re-login), so nothing was verifiable through the live tile. Inferred and flagged for audit: committed straight to `master` with `git.allow_default_branch_commits` unset, on the strength of every prior quick task doing so and the standing keep-`master` preference
Older activity: 2026-09-10 -- Quick task 260910-p91: two independent changes. **(1)** The alt-mode `no_usb_pd` row now states a condition instead of issuing an instruction -- "use a USB-C port and cable that support Power Delivery" became **"Alt mode did not start -- this connection does not provide USB Power Delivery, which needs both a port and a cable that support it"**. This was the last remedy-shaped string in the UI, and `260910-o99` had flagged rather than swept it for a reason that still holds: here the daemon *names* the cause (`usb_altmode_state == Unsuccessful` with `no_usb_pd` in `usb_altmode_failure`), so unlike the three withdrawn claims the information is sound -- a PD link definitionally needs a capable port AND a capable cable. What was wrong was only the **mood**: this machine has no PD controller at all (`/sys/class/typec` absent), so the imperative resolved to "buy a different motherboard", telling the reader to do something they may have no path to. Restated, therefore, not deleted -- same gate, same amber treatment, same SVID naming, voice matched to the prior three ("did not start" mirrors the connector hint's "did not come up"). A full sweep of every `_()` in `src/` and `prefs.js` for imperative verbs returns exactly one hit and it is a noun (`_('Plug orientation')`), so this really was the last. Three comments asserting the remedy as fact were corrected so the belief is not re-seeded -- the same failure `260910-o99` fixed in four `notifier.js` comments. `deriveAltMode().actionable` kept (four assertions pin it) but redocumented as a row *selector*, not a claim a fix exists. The new CI guards deliberately inspect the extracted `_('...')` **literals** rather than the file text: the docstring and Translators comment quote the withdrawn imperative on purpose, and a substring test would have failed on the very comments that keep the decision from being forgotten. **(2)** CLAUDE.md's EGO framing corrected. USBee is **not published on extensions.gnome.org**, yet Release Process step 6 instructed the maintainer to download the zip and submit it at extensions.gnome.org/upload -- work that does not happen. Step 6 is gone; the release ends at the GitHub Release `release.yml` already creates (that workflow never mentioned EGO, which makes step 6 a pure documentation error, not a process one). The correction is narrow by design: the constraints EGO motivated -- no bundled binaries, no minified JS, documented API surface only -- are good on their own merits and cost nothing to keep, so only their *status* changed. A new **Distribution status** subsection states the position once and instructs the reader to read every other EGO mention through it; a new **Distribution** constraint bullet carries the same sentence; and a single preamble above "What NOT to Use" governs a dozen "EGO reviewers will reject it" cells rather than rewriting each one. Wording is **neutral on intent** -- the user has not said whether submission is abandoned or deferred, so every new sentence is true under both readings. The mirrored edits were made in `.planning/PROJECT.md` and `.planning/research/STACK.md` as well and verified byte-identical, because those CLAUDE.md sections are GSD-generated mirrors that the next docs-update would otherwise revert. README.md **did** misinstruct a user -- it headed the install section "From extensions.gnome.org (recommended)" and claimed a listing was pending review -- and was fixed; `ci.yml`'s "EGO pre-check (shexli)" job was left alone, since a lint that runs and passes is exactly the kept-by-choice posture. 391 assertions green (94/66/174/57) under `rtk proxy`; `.pot` delta verified rather than assumed at exactly -1/+1 with 153 unique msgids on both sides and the header intact; packed + installed (25 files / 240,775 B, 15 `src/` entries, `ls -ld` pre-flight confirmed a real directory). metadata.json untouched at 2.7.1 / 12. No version bump, no tag, no push, Shell not restarted
Oldest tracked: 2026-09-10 -- Quick task 260910-myu: USBee stopped telling correctly-installed users that usbeehive is **not installed**. The unit-file probe stat()ed three paths, and the one usbeehive's own installer writes to -- `$XDG_CONFIG_HOME/systemd/user`, systemd's *highest-priority* user unit path, per `user_unit_dir()` in `../usbeehive/src/bin/usbeehived.rs` -- was not among them, so every stopped-but-installed daemon routed to a `cargo install` the user had already run. New `src/service-probe.js` replaces the boolean with a tri-state (`NOT_INSTALLED` / `SERVICE_MISSING` / `INSTALLED`, the middle one being "cargo install done, `--install-service` not"), searches systemd's real path, then asks systemd `GetUnitFileState` asynchronously to correct itself. The command line stopped truncating (`St.Entry` is single-line and never reflowed -- `d6806ed` had only fixed the `St.Label`s beside it), and both the popover and the prefs window gained a **Start** button: async `StartUnit` on the session bus, never a `Gio.Subprocess`, with the D-Bus refusal shown verbatim and an 8 s watchdog for the daemon that starts and immediately exits. On the (a) extension-side-start vs (b) daemon-side-D-Bus-activation fork: (b) is the durable fix and is written up for usbeehive rather than half-built here; (a) shipped because it is a complete feature, works on every usbeehive that exists today, and covers a failing daemon that activation cannot explain. Opening the *real* prefs window with the daemon stopped caught a pre-existing bug nobody reported: `&&` in a command breaks Pango markup parsing in Adwaita subtitles, so every command row had been failing to render. No version bump, no tag, no push; packed + installed (25 entries / ~231 KB, real-directory pre-flight), Shell not restarted

## Performance Metrics

| Metric | Value |
|--------|-------|
| Total requirements | 55 (34 v1.0 + 5 v1.1 + 16 v2.0) |
| Mapped to phases | 55 (100%) |
| Orphaned | 0 |
| Phases | 4 (3 complete, 1 planned + ready) |
| v2.0 plans queued | 3 (5 + 14 + 8 = 27 tasks) |
| v2.0 plans complete | 0 |
| Plan-checker | PASS (post B-1/B-2 fix, 2026-05-14) |
**Per-Plan Metrics:**

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 260910-r46 P01 | 1m 33s | 3 tasks | 2 files |

## Accumulated Context

### Decisions Locked

- **Stack:** Pure-GJS GNOME 46+ Shell extension, ESM-only. No Rust binary, no GTK4-rs, no companion service. (See `.planning/research/STACK.md`.)
- **License:** GPL-3.0 (matches GNOME ecosystem norms; explicitly different from `usbeehive`'s permissive license).
- **Distribution:** extensions.gnome.org (EGO) as a single zip produced by `gnome-extensions pack`. No Flatpak for the extension itself.
- **Min target:** GNOME Shell 46. `metadata.json` `shell-version` will declare `["46", "47", "48"]`.
- **Settings storage:** GSettings schema `org.gnome.shell.extensions.usbee`. No TOML, no dotfiles. (EGO `shexli` lint requires the `org.gnome.shell.extensions.*` namespace for extension settings schemas; renamed from the original `us.bitcreed.usbee` in quick task `260514-mq0` on 2026-05-14.)
- **i18n:** English strings only in v1; every user-visible string wrapped in `gettext` `_()` markers from day one. Translations deferred to v2.
- **Architecture rule:** All USB knowledge flows through `usbeehive` via D-Bus. USBee performs no `/sys` or udev access of its own.
- **D-Bus wire names (Plan 01):** `BUS_NAME='org.usbeehive.Devices'` and `OBJECT_PATH='/org/usbeehive/Devices'` (NO trailing 1); `INTERFACE_NAME='org.usbeehive.Devices1'` (the `1` is only on the interface). Verified against `../usbeehive/src/dbus.rs:290-292`; CONTEXT.md's original wording had this wrong.
- **Plan 01 IFACE_XML source (Plan 01 Task 2):** Used RESEARCH.md Option B (hand-written XML) because the daemon was not running on the dev machine and no pre-built binary was available. Every member signature verified against `../usbeehive/src/dbus.rs:197-292`. Capture a fresh `busctl --user introspect` XML when the daemon is later running and diff against the committed version.
- **Plan 01 stubs handed to Plan 02:** `DeviceStore.subhead` (Plan 02 Task 1 swaps in D-09 derivation), `populateDeviceRows` (Plan 02 Task 3 swaps in full LIST-01..06 + DIAG-01/02), and `DBusClient._onAppeared` lacks `DeviceAdded/Removed` subscriptions + 150 ms debounce (Plan 02 Task 2 adds them). Module interfaces are unchanged.
- **Devices2 cross-team spec locked (2026-05-13):** Outcome of multi-round negotiation with usbeehive; full wire shape, machine-key vocabulary, enum extensibility convention, and "NO backwards compatibility" hard rule recorded in `.planning/todos/pending/2026-05-14-migrate-to-usbeehive-devices2-wire-shape.md` (commit 724c22a). Daemon implementation matches the spec verbatim (`../usbeehive` commit `5e216cd`).
- **Devices2 INTERFACE_NAME bump:** `org.usbeehive.Devices1` → `org.usbeehive.Devices2`. `BUS_NAME` and `OBJECT_PATH` unchanged. Hard cut; the previous "supports both" was rejected upstream and downstream.

### Open Questions (v2.0 — surface during Plan 04-01)

- ✅ **CONFIRMED 2026-05-14:** `MIN_USBEEHIVE_VERSION = "0.6.0"`. usbeehive cut release 0.6.0 (Cargo.toml `0.6.0`, release commit `1258de4` "Release 0.6.0 — Devices2 wire (breaking)") — no remaining ambiguity for the version pin.
- `primary_driver == ""` UI treatment: badge on the device row vs. note in the detail panel vs. ignore for v2.0. Decided during Plan 04-01.
- `device_subclass` rendering: append to row title (`"Storage · SSD"`), surface only in the detail panel, or ignore for v2.0 (subclass-aware icons explicitly out per CHANGELOG). Decided during Plan 04-01.
- Adwaita symbolic icon picks for the four daemon `device_class` variants without an obvious fit: `SmartcardReader`, `Bluetooth`, `Serial`, `VideoCapture`. Decided during Plan 04-01 icon audit.

### Todos

- `.planning/todos/pending/2026-05-14-migrate-to-usbeehive-devices2-wire-shape.md` — full v2.0 phase context (locked wire spec + hard "no compat" rule). Tagged for Phase 4 in step 10.5.
- ~~`.planning/todos/pending/2026-05-15-show-warning-badges-and-in-panel-charging-diagnostics-in-pop.md`~~ — **Done (2026-05-26, commit 1306c09).** Daemon-side `ChargerLimit` dead variant deferred upstream.

### Blockers

()

- Pre-Devices2 orphan `.planning/phases/01-tile-popover-hotplug-daemon-missing-state-v0-1/01-RESEARCH.md` (1386 lines, untracked) was wiped by `gsd-sdk query phases.clear` at milestone switch (2026-05-14). Never committed; not in git, not on disk anywhere on the host, no SDK archive. Genuinely unrecoverable. Acknowledged 2026-05-14 — historical-only value, Phase 01 closed, not blocking anything.

### Risks Being Carried

- **EGO review hazards:** bundled binaries, log spam, excessive `try`/`catch` (December 2025 AI-code rule), synchronous D-Bus calls, private `_addItems`-style API. Mitigations baked into Phase 1 architecture per `.planning/research/PITFALLS.md`.
- **GJS lifecycle discipline:** lock/unlock cycle is the highest-yield manual QA gate. Mandatory before each phase exit.
- **Notification spam:** Phase 2 must implement `replaces_id` keyed by stable port ID, `CloseNotification` on `CapabilityRestored`, 2–3 s suppression window after `NameOwnerChanged` `null→owner`, and a hard daily rate cap per port.
- **Upstream dependencies pushed to `usbeehive`:** stable per-port identifier, optional `DevicesChanged` atomic-snapshot signal, optional `LastDegradationTimestamp`. Track upstream; do not work around in USBee.

## Session Continuity

**Last session:** 2026-09-11T01:41:18.603Z
**Stopped at:** Completed quick task 260910-r46 (v2.8.0 packed + installed locally; Shell restart outstanding)
**Resume file:** None

### Latest Session

- **2026-05-14 — Milestone v2.0 opened + Phase 04 roadmapped:**
  Cross-team Devices2 wire-shape spec locked through multi-round negotiation with usbeehive (recorded as a pending todo); usbeehive shipped the matching daemon implementation on master (`5e216cd`). Captured the migration intent + locked spec + "NO backwards compatibility" hard rule as a phase seed todo (`724c22a`). Switched PROJECT.md to v2.0, reset STATE.md frontmatter via `state.milestone-switch`, cleared the previous milestone's phase directories, added Phase 04 to ROADMAP.md with 3 plans (prep & UX, wire cutover, release), added 16 v2.0 requirements (WIRE × 4, CLEAN × 3, DISP × 5, COMPAT × 2, REL × 3) to REQUIREMENTS.md with full traceability.

- **2026-05-13 — Phase 03 complete:**
  v1.1 UI rework shipped — accordion popover, class icons, issue-first sort, Adwaita-coherent detail panel. EGO not submitted (held for v2.0 first submission per Devices2 release strategy).

- **2026-05-12 — Phase 02 complete:**
  Notifications + preferences + EGO scaffolding shipped as v1.0; zip held from EGO submission.

- **2026-05-11 — Phase 01 complete + roadmap created:**
  Walking skeleton + live device list landed under `usbee@bitcreed.us/`. Tile, popover, hotplug, daemon-missing state all working.

### Next Action

**Release v2.1.0 → first EGO submission.**

v2.0.0 was tagged and pushed to GitHub but never submitted to EGO. The warning badge feature (commit 1306c09) was added after the tag, so the first EGO submission will be v2.1.0.

Steps:

1. Bump `metadata.json` `version-name` → `"2.1.0"`, `version` → `3`.
2. Write `## [2.1.0]` CHANGELOG entry (warning badge + charging diag rows).
3. Regenerate `.pot` (already done in 1306c09 — re-run after any further string churn).
4. `gnome-extensions pack` + smoke-test + create annotated tag `v2.1.0`.
5. Push tag → release.yml builds zip + GitHub Release.
6. Upload zip to https://extensions.gnome.org/upload/ — first ever EGO submission.

**Pending — post-EGO-upload only (do NOT block submission on this):**

- **Update repo `homepageUrl` once the EGO extension ID is assigned.** Set it to a placeholder `https://extensions.gnome.org/extension/` during `gh repo create`, but EGO URLs use `https://extensions.gnome.org/extension/<id>/<slug>/`. After uploading to https://extensions.gnome.org/upload/ and getting the assigned ID, run:
  ```bash
  gh repo edit abrauchli/usbee --homepage "https://extensions.gnome.org/extension/<ID>/<slug>/"
  ```
  Doesn't affect EGO submission itself — purely cosmetic for the GitHub repo page.

### Files of Record

- `.planning/PROJECT.md` — project charter (now reflects v2.0 milestone)
- `.planning/REQUIREMENTS.md` — 55 requirements with traceability table
- `.planning/ROADMAP.md` — 4 phases, success criteria, coverage map
- `.planning/todos/pending/2026-05-14-migrate-to-usbeehive-devices2-wire-shape.md` — v2.0 phase seed: locked Devices2 wire spec + "no backcompat" rule
- `.planning/research/SUMMARY.md` — synthesized v1.0 research (still authoritative for the GJS / GNOME stack; Devices2 migration adds no new stack pieces)
- `.planning/config.json` — coarse / yolo / parallel
- `../usbeehive/src/dbus.rs` — authoritative wire shape for Devices2 (consult during Plan 04-02)
- `../usbeehive/CHANGELOG.md` — `[Unreleased]` section carries the regex → field migration table + property-key vocabulary

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260514-g4x | For the bubble-tile icon use drive-harddisk-usb-symbolic instead of the shipped one | 2026-05-14 | a2bc54c | [260514-g4x-for-the-bubble-tile-icon-use-drive-hardd](./quick/260514-g4x-for-the-bubble-tile-icon-use-drive-hardd/) |
| fast | also use that icon in the expanded tile's header and as header text next to the icon use "{n} USB devices" | 2026-05-14 | 17d9906 | — |
| fast | Hub icon → network-proxy-symbolic (bundled SVG, Gio.FileIcon path) | 2026-05-14 | 4a2ab03 | — |
| fast | Storage→media-removable-symbolic; daemon version format; Show USB Hubs pref | 2026-05-14 | c5ce0b1 | — |
| 20260514-screenshot-and-release-prep | Add screenshot to README, move v2.0.0 tag to HEAD, push | 2026-05-14 | 8ea3c50 | [20260514-screenshot-and-release-prep](./quick/20260514-screenshot-and-release-prep/) |
| 260514-mq0 | Fix 3 EGO shexli lint errors: rename GSettings schema to org.gnome.shell.extensions namespace, move test file out of extension dir | 2026-05-14 | 3172694 | [260514-mq0-fix-ego-shexli-lint](./quick/260514-mq0-fix-ego-shexli-lint/) |
| 260526-warning-badges | Warning badge (amber border) on is_warning popover rows + charging_diag.summary/detail in expanded detail panel | 2026-05-26 | 1306c09 | [260526-warning-badges-charging-diag](./quick/260526-warning-badges-charging-diag/) |
| 260526-c6p | Device-change notifications (DeviceAdded/Removed toasts, scope enum all/power/off) + Show technical details toggle gating 8 advanced property rows | 2026-05-26 | 286c85f | [260526-c6p-connect-disconnect-notifications-tech-de](./quick/260526-c6p-connect-disconnect-notifications-tech-de/) |
| 260526-dmj | Devices3 wire migration (MIN_USBEEHIVE 0.7.0, pdo_list/active_pdo_index) + cable trust row + transport pill strip + structured Charger PDOs block | 2026-05-26 | ed7acd6, c130b50, 6cad52f | [260526-dmj-devices3-wire-migration-pdo-list-trust-s](./quick/260526-dmj-devices3-wire-migration-pdo-list-trust-s/) |
| 260526-i7q | Three-way daemon-state hints (not-installed / not-running / out-of-date) with `usbeehived --install-service` and `cargo install usbeehive --features=dbus` commands; shorter notify-changes ComboRow item labels | 2026-05-26 | 4b8c7bb, dec4dbb | [260526-i7q-daemon-status-hints-shorter-notify-chang](./quick/260526-i7q-daemon-status-hints-shorter-notify-chang/) |
| 260608-hug | Fix clipped empty-state title ("u...") — move titles into the vertical body box (hide built-in horizontal PopupMenuItem label) across all three daemon-state builders + `.usbee-empty-state-title` CSS rule | 2026-06-08 | 3229281, c6a2d43 | [260608-hug-fix-clipped-empty-state-title-u-in-quick](./quick/260608-hug-fix-clipped-empty-state-title-u-in-quick/) |
| 260616-c18 | Subscribe to the daemon's additive `DeviceChanged` signal and `_scheduleRefresh()` the tile (no notification) — consumer half of the stale "Charging 15W" after-unplug fix; byte-equal IFACE_XML/dbus-iface.xml preserved | 2026-06-16 | 5d6f6a4, 0395206, 10ce59d | [260616-c18-subscribe-to-devicechanged-signal-and-re](./quick/260616-c18-subscribe-to-devicechanged-signal-and-re/) |
| 260821-ke2 | Store-owned daemon tri-state so the pill reads "Daemon out of date" instead of contradicting the popover; required + detected usbeehive version surfaced in popover and prefs; copy buttons on every displayed command; new zero-import `src/daemon-status.js` shared across both processes + CI | 2026-08-21 | 3346345, 32760d7, 02a14c4 | [260821-ke2-show-daemon-out-of-date-in-the-tile-pill](./quick/260821-ke2-show-daemon-out-of-date-in-the-tile-pill/) |
| fast | EGO AI-reference pass: drop non-throwing try/catch around destroy()/disconnect()/dispose() in notifier.js, popover.js, signal-registry.js (+ orphaned `kind` field) | 2026-08-21 | c3db795 | — |
| 260822-hkn | Document the canonical `gnome-extensions pack` invocation (`--podir` + both `--extra-source` flags) in CLAUDE.md as shared source of truth with release.yml; warn that a wrong `--extra-source` is silently ignored and yields a `src/`-less zip; add local install + on-disk verification steps and the `gnome-extensions info` staleness caveat | 2026-08-22 | fc3c574 | [260822-hkn-document-pack-and-local-install](./quick/260822-hkn-document-pack-and-local-install/) |
| 260905-b0s | Consume usbeehive's additive BOS + connector/power/quirks/hwdb waves on the unchanged Devices5: per-device Link row + capability verdict, `port.peer_state` explanation, hub occupancy/bus power, tile issue tier, `DataRateDegraded`/`DataRateRestored` notifications + `data-rate-mutes`, `product_db`, `DaemonState.TOO_NEW`. **Reverses LOCKED decision 260526-c6p D-2** — unknown property keys are now tech-gated (24 new daemon keys would otherwise render as a property dump). Three new zero-import modules (`property-policy.js`, `link-verdict.js`, `notify-policy.js`) and a revived, CI-wired `tests/forward-compat.test.js` | 2026-09-05 | 168790e, ed57431, 9033390, f3ed164, 59e6c5c, e09e2dd | [260905-b0s-bos-trim-consumer-ui](./quick/260905-b0s-bos-trim-consumer-ui/) |
| 260910-ggy | Property-panel values no longer truncate: `St.Label`'s default `PANGO_ELLIPSIZE_END` was silently disabling the `line_wrap` `buildPropertyRow()` already asked for (Pango ellipsizes at line one when no layout height is set), so every value rendered as one cut-off line. Cleared on both the value and the key label (+ `y_align: START`), same nine-label fix in `empty-state.js`, and a CI guard on the `line_wrap`/`ellipsize` pairing | 2026-09-10 | 683708f, d6806ed, cedb4e8 | [260910-ggy-fix-ellipsized-value-text-in-the-device-](./quick/260910-ggy-fix-ellipsized-value-text-in-the-device-/) |
| 260910-hrj | Release v2.7.1 prepared (metadata 2.7.1/12 + changelog; **untagged and unpushed** on purpose -- the tag fires the release workflow), packed and installed locally so the property-panel wrap fix is live at next login. Found and fixed two wrong facts in CLAUDE.md's release procedure: the "21 entries / ~140 KB" zip check was stale (a correct zip is 24 / ~198 KB, and grows with every `src/` module), and `gnome-extensions install --force` deletes through a symlinked dev install -- it wiped this repo's `usbee@bitcreed.us/` sources mid-run (restored with `git checkout`; nothing uncommitted was lost, and the mechanism was reproduced against a throwaway uuid rather than guessed). The symlinked dev install is now a real directory. | 2026-09-10 | 116c916, c6c7d38 | [260910-hrj-prepare-release-v2-7-1-version-bump-chan](./quick/260910-hrj-prepare-release-v2-7-1-version-bump-chan/) |
| 260910-lyx | Withdrew the three socket-level connector claims (`ss-never-linked` / `ss-unstable` / `ss-elsewhere`) that 2.7.0 derived from the kernel's USB2↔USB3 root-port `peer` link. That link comes from ACPI `_PLD` (exact `location` match) or a bare port-index guess, with **no provenance recorded anywhere in sysfs** — and `_PLD` is confidently wrong on this machine, where the sole USB-C socket is wired D+/D- to `usb5-port2` but its SuperSpeed lanes to `usb6-port1`. A healthy USB 3 hub on that socket would have been reported as "a USB 2-only cable". Checked the wire before choosing a fix: usbeehive 0.12.0 drops root hubs from summaries (`src/sysfs/manager.rs:420-423`) and never reads port `location`, so the pairing-free controller-level reformulation was **not available** — the claim was withdrawn, not re-derived. One cause-agnostic token replaces three; the `port.peer_state` **presence** gate stays (pure narrowing, nothing newly speaks) while its **value** now selects nothing, pinned by a regression test over all seven kernel states. Folded into the untagged 2.7.1 changelog; no version bump, no tag, no push. Packed + installed locally (24 entries / ~200 KB; `ls -ld` pre-flight confirmed a real directory, sources intact after). | 2026-09-10 | 18aff96, 2426820 | [260910-lyx-fix-ss-never-linked-false-positive-kerne](./quick/260910-lyx-fix-ss-never-linked-false-positive-kerne/) |
| 260910-mes | v2.7.1 **published**. Release mechanics only, no source edits. Pre-flight cleared four gates before anything left the machine — metadata `2.7.1`/`12`, clean tree bar untracked `.claude/`+`.gsd/`, the release workflow's *own* awk yielding the full 39-line 2.7.1 body (boundaries checked against the `## [2.7.0]` header and the `[2.7.1]:` link ref), and ten expected commits carrying no zip or secret material. Then `master` (`dc6561e..e34e0d8`), annotated tag `v2.7.1`, tag push, and `release.yml` green in 33s. Published zip verified by download, not by trust: 24 entries / 204,819 B with `src/` and all 13 modules intact. Trap recorded for the next release: `git tag -a -F <file>` defaults to `--cleanup=strip`, which silently ate the `### Fixed` markdown header as a comment — recreated with `--cleanup=verbatim` before pushing (and `-m` cannot be combined with `-F`). EGO upload deliberately left to the human. | 2026-09-10 | e34e0d8 (tagged v2.7.1) | [260910-mes-tag-and-push-the-v2-7-1-release](./quick/260910-mes-tag-and-push-the-v2-7-1-release/) |
| 260910-myu | "usbeehive not installed" was being shown for a daemon that was installed, enabled and merely stopped. `isUsbeehiveServiceInstalled()` stat()ed three unit paths and omitted `$XDG_CONFIG_HOME/systemd/user` — where usbeehive's own `--install-service` writes (`user_unit_dir()`, `../usbeehive/src/bin/usbeehived.rs:279-291`) and systemd's *highest-priority* user unit path. New gi-only `src/service-probe.js` (loads in the Shell process, the prefs process and bare-gjs CI) replaces the boolean with `NOT_INSTALLED` / `SERVICE_MISSING` / `INSTALLED` — the middle state being "`cargo install` done, `--install-service` not", which was also being told to reinstall — searches systemd's real path, then corrects itself from systemd `GetUnitFileState` asynchronously. Separately, the command line was still truncating: it was an `St.Entry`, which is single-line and does not reflow, so `d6806ed` (nine `St.Label`s) had not touched it; now a selectable wrapping `St.Label` (`WrapMode.WORD_CHAR`, code-block CSS, copy button kept). Both surfaces gained a **Start** button — async `StartUnit` on `org.freedesktop.systemd1` on the *session* bus, never `Gio.Subprocess`/`systemctl`, with the D-Bus refusal shown verbatim (GDBus prefix stripped) and an 8 s watchdog for the daemon that starts and immediately exits. On the (a) extension-side vs (b) D-Bus-activation fork: **(b) is the durable fix and was written up for usbeehive, not half-built here** (`260910-myu-USBEEHIVE-ASK.md`); (a) shipped anyway because it is complete on its own, works on every usbeehive that exists today, only fires on a method call USBee never makes into an unowned name, and covers a daemon that dies on start. Live-testing the *real* prefs window with the daemon stopped caught an unreported pre-existing bug: `&&` breaks Pango markup parsing in Adwaita subtitles, so every command row had been failing to render. No version bump, no tag, no push. | 2026-09-10 | 4f88621, a1212ff, 53fb967, d7f24ef, 9f09baa, 3c1a3b6, da4ad71, 1e7f8de | [260910-myu-fix-daemon-status-misreport-ellipsized-e](./quick/260910-myu-fix-daemon-status-misreport-ellipsized-e/) |
| 260910-n10 | Split the property panel's three-line run-on `Link` row into one fact per row: `Link  480 Mb/s` and a new `Capability  SuperSpeed 5 Gbps`. The string was **extension-composed**, not daemon-supplied (`popover.js:451`), so the unsupportable half could be withdrawn here: "could run at X **on a faster port**" is wrong for the very device that prompted it — a Realtek RTL8153 capped by a USB-2.0-only Genesys GL850 (`05e3:0608`) *inside* a Samsung monitor, where no port on the machine can help. USBee cannot see the ancestor chain (needs the daemon's proposed `link.culprit`), so the new row states the device's own BOS-declared capability and **stops** — same discipline as `18aff96`. Label is `Capability`, reusing the word `label-table.js` already binds to `usb_bos_suppressed`'s complementary "capability unknown" value (mutually exclusive states, one concept, **zero new translatable strings**); the user's `Achievable` sketch was rejected precisely because it asserts the speed is reachable somewhere. Row suppressed unless capability *exceeds* the link (numeric twin decides; verdict is the fallback), so `AtCapability` adds no noise. `(USB 2.1)` also dropped from the row — it is canonicalised `bcdUSB 2.10`, which declares only that a BOS descriptor exists; USB 2.1 is not a specification, and it read like an actionable version number. Wire values untouched. 167 assertions green across all three CI suites; `.pot` regenerated (two msgids withdrawn, none added). No version bump, no pack, no install. | 2026-09-10 | 11907fd, 6d5245e, 39da26a | [260910-n10-split-the-run-on-link-row-into-a-factual](./quick/260910-n10-split-the-run-on-link-row-into-a-factual/) |
| 260910-nge | Integrated the two quick tasks that ran in parallel off the v2.7.1 commit `ceb643e`: merged 260910-n10 (Link/Capability split) into 260910-myu (daemon-state probe, wrapping command row, prefs Start button). Integration, not review -- no decision of either task revisited, and **no conflict of intent** between them; they touch adjacent concerns in one file. `popover.js` merged clean because n10 stayed inside `buildLinkBlock()`. Three conflicts, not the two predicted -- `.planning/STATE.md` also collided, on both the narrative block and the quick-task table. `CHANGELOG.md` resolved by keeping **both** entry sets whole in one `## [Unreleased]` section, ordered Added / Changed / Fixed (the repo has no fixed subsection order -- 2.7.0 runs Changed->Added, 2.6.0 Fixed->Added->Changed -- so Keep a Changelog was the tie-break). The `.pot` was regenerated, never hand-merged, and the invocation was **derived by reproducing the committed template byte-for-byte** rather than taken on trust: `xgettext --from-code=UTF-8 --package-name=USBee --package-version=2.7.1 -o po/usbee@bitcreed.us.pot usbee@bitcreed.us/prefs.js usbee@bitcreed.us/src/*.js`. **Input file order is load-bearing** -- `prefs.js` must come first; listing `src/*.js` first (as CLAUDE.md's tooling table implies) reorders every msgid block and fabricates a several-hundred-line diff. Union verified rather than assumed: 146 unique msgids = base 138 + myu's 12 additions, less myu's 2 and n10's 2 deliberate withdrawals; deltas disjoint, zero msgids in neither parent, `msgfmt --check-format` clean. Also caught that the template was already stale on line refs pre-merge (`9f09baa` generated it, `da4ad71` then grew `prefs.js`). 384 assertions green (dbus-client 66, daemon-status 94, forward-compat 167, service-probe 57), run under `rtk proxy` so no `test result:` line was filtered away. Packed + installed: 25 entries / 235,168 B with all 14 `src/` modules; `ls -ld` pre-flight confirmed a real directory and repo sources were intact afterwards. metadata.json untouched at 2.7.1 / 12. No version bump, no tag, no push, Shell not restarted. | 2026-09-10 | 6047da5 | [260910-nge-integrate-the-link-capability-quick-task](./quick/260910-nge-integrate-the-link-capability-quick-task/) |
| 260910-o99 | Withdrew the "move it to a faster port" remedy from the **notification** for a degraded link, and from the property panel's surviving **Fix** row. Unlike `260910-n10`'s panel string, the notification body was **daemon-supplied** — usbeehive composes it at `src/bos.rs:824` as "Device advertises up to X; move it to a faster port or use a cable that supports it" and USBee passed it through verbatim, so the only decision USBee owned was whether to render it. It no longer does, and it deliberately does **not** split the sentence on its `;` — that would invent a claim boundary the wire does not define, so the daemon's factual half went with the remedy (nothing lost: the title already carries both rates, and the panel keeps the `Capability` row). The body is now composed under gettext: "The cause could be the cable, the port, or a hub in between" — exhaustive without any topology, because `Degraded` means the device's own descriptor advertises more than it negotiated, so by its own account the device is not the limit. The panel's `Fix` row ("Move it to a USB 3 port or use a cable that supports it") was **extension-composed** and survived `260910-n10` only because that task was scoped to `BelowCapability` and said so; the evidence is no better for `Degraded`, and the live case is an RTL8153 capped by a GL850 *inside* a monitor where relocating it is exactly wrong. **The notification still fires, unchanged at `Urgency.NORMAL`, persistent and coalesced** — the gate is the device's own `bFunctionalitySupport` declaring it will not work properly at this speed (a malfunction report, not advice), it lands at plug time when the user is holding the cable, and `Urgency.LOW` was rejected after verifying against the installed gnome-shell 48 (`_onNotificationRequestBanner` returns early for LOW → no banner at all), which would trade a false remedy for a false calm; research rev 2 §8 also pins `Degraded` policy as-is, reserving the softer default-off tier for future `link.fixable` suggestions. Action buttons audited and unchanged (they name what they do to USBee, not what to do to the hardware). Charging `CapabilityDegraded` still renders the daemon's detail — audited, every usbeehive charging string is an observation (`src/diagnostic.rs:96-140`) — with a comment asking for a re-check if that copy changes. The alt-mode `no_usb_pd` remedy (`popover.js:606`) was found, judged materially different (the daemon *names* the cause) and **left standing, flagged for audit**. 4 new CI guards, one slicing `_emitDataRateDegraded()` out by hand so the legitimate charging path cannot satisfy it by accident; 388 assertions green (daemon-status 94, dbus-client 66, forward-compat 171, service-probe 57) under `rtk proxy`. `.pot` delta exactly as intended: 155 → 154 unique, −2 / +1, header intact, `msgfmt --check-format` clean. Packed + installed (25 files / 239,190 B, 15 `src/` entries; `ls -ld` pre-flight confirmed a real directory), on-disk metadata still 2.7.1 / 12. No version bump, no tag, no push, Shell not restarted. | 2026-09-10 | 67d5b27, f3f8c60, 09b1c5c | [260910-o99-withdraw-the-false-faster-port-remedy-fr](./quick/260910-o99-withdraw-the-false-faster-port-remedy-fr/) |
| 260910-p91 | Two independent changes. **(1)** Restated the alt-mode `no_usb_pd` row as a condition rather than an instruction: "use a USB-C port and cable that support Power Delivery" -> **"Alt mode did not start -- this connection does not provide USB Power Delivery, which needs both a port and a cable that support it"**. This was the last remedy-shaped string in the UI (`18aff96` took the connector hints, `260910-n10` the Link/Capability split, `260910-o99` the notification body and the `Fix` row), and `260910-o99` had flagged it rather than swept it in **for a reason that still holds**: the cause here is daemon-*named* (`usb_altmode_state == Unsuccessful` and `usb_altmode_failure` contains `no_usb_pd`), so unlike the withdrawn claims the information is sound -- a PD link definitionally needs a capable port AND a capable cable. Only the **mood** was wrong: this machine has no PD controller at all (`/sys/class/typec` absent), so the imperative resolved to "buy a different motherboard". Hence restated, not deleted -- same gate, same amber class, same SVID naming. Voice matched to the three prior tasks ("did not start" deliberately mirrors the connector hint's "did not come up"). **Sibling sweep proves this was the last one**: every `_()` in `src/` + `prefs.js` grepped for imperative verbs returns one hit, `_('Plug orientation')`, a noun; the alt-mode value strings in `label-table.js` are already bare facts. Three comments asserting the remedy as fact were corrected (popover.js call site + `buildAltModeBlock()` docstring, `deriveAltMode()` docstring) -- the same re-seeding failure `260910-o99` fixed in four notifier.js comments. `deriveAltMode().actionable` **kept** (4 assertions pin it) but redocumented as a row selector, not a claim a fix is available. New CI guards inspect the extracted `_('...')` **literals**, not the file text, because the docstring and Translators comment quote the withdrawn imperative on purpose -- a substring test would have punished the record for saying what was rejected. **(2)** Corrected the EGO framing in CLAUDE.md: USBee is **not published on extensions.gnome.org** and Release Process step 6 was directing work that does not happen ("download the zip and submit it at extensions.gnome.org/upload"). Step 6 deleted -- the release now ends at the GitHub Release the workflow already creates (`release.yml` never had an EGO reference; step 6 was a pure doc error). Narrow, not wholesale: a new **Distribution status** subsection states the position once and tells the reader to read every other EGO mention through it, a new **Distribution** constraint bullet carries the same sentence, and **one preamble** above "What NOT to Use" governs a dozen "EGO reviewers will reject it" cells rather than rewriting each -- the guidance stands on its own merits and keeping it leaves submission possible at zero cost. **Deliberately neutral on intent** -- every new sentence is true whether submission is abandoned or deferred. Mirrored edits were made in `.planning/PROJECT.md` and `.planning/research/STACK.md` too and verified byte-identical, because those CLAUDE.md sections are GSD-generated mirrors and the fix would otherwise be reverted by the next docs-update. README.md **did** misinstruct (headed the install section "From extensions.gnome.org (recommended)" + "EGO listing pending review") and was fixed; `ci.yml`'s "EGO pre-check (shexli)" job left as is -- a lint that runs and passes *is* the kept-by-choice posture. 391 assertions green (94/66/174/57) under `rtk proxy`. `.pot` delta verified not assumed: exactly -1/+1, 153 unique msgids both sides, 27/27 lines, header intact, `msgfmt --check-format` clean. Packed + installed (25 files / 240,775 B, 15 `src/` entries; `ls -ld` pre-flight confirmed a real directory, repo tree clean afterwards); metadata still 2.7.1 / 12. No version bump, no tag, no push, Shell not restarted | 2026-09-10 | 223747f | [260910-p91-restate-alt-mode-power-delivery-string-a](./quick/260910-p91-restate-alt-mode-power-delivery-string-a/) |
| 260910-r46 | Released **v2.8.0** and deployed it locally — no tag, no push, by design. `usbee@bitcreed.us/metadata.json` bumped `version` 12 → **13** and `version-name` 2.7.1 → **2.8.0** (both edits length-preserving, which is why the packed zip stayed byte-identical in size to `260910-p91`'s). `CHANGELOG.md`'s populated `## [Unreleased]` section — Added, Changed *and* Fixed subsections, hence a **MINOR** bump rather than a patch — was rolled verbatim into `## [2.8.0] — 2026-09-10` with a fresh empty `## [Unreleased]` left above it and a `[2.8.0]:` reference link appended in the file's existing `releases/tag/vX.Y.Z` style. The file has **no `[Unreleased]:` compare link** and one was deliberately *not* invented. Line count pinned at **680** (677 + 2 body + 1 link ref) as a deterministic guard against a lost or duplicated section that a heading-only grep would miss. Packed with the full CLAUDE.md invocation (`--podir=../po --extra-source=src --extra-source=icons --force`) and the zip *proved* good rather than assumed: **25 entries / 240,775 B with 15 `src/` entries** — the `src/` prefix, not the totals, being the real gate, since a wrong `--extra-source` path exits 0 and prints nothing. Installed with `--force` after an `ls -ld` pre-flight re-confirmed the destination is a real directory (the `260910-hrj` symlink hazard would otherwise wipe the repo's extension sources); repo tree verified intact afterwards. On-disk metadata read back **under `rtk proxy`** — `"version": 13` / `"version-name": "2.8.0"` — deliberately *not* via `gnome-extensions info`, which reports stale in-process metadata until the Shell restarts. `git tag -l v2.8.0` returns nothing and `origin/master..HEAD` is 25 commits unpushed, both as intended. **GNOME Shell restart outstanding** (Xorg: Alt+F2 → r; Wayland: full re-login) — 2.8.0 is on disk but the Shell still runs 2.7.1 from memory, so nothing was verifiable through the live tile or popover. Inferred decision flagged for audit: committed directly to `master` despite `git.allow_default_branch_commits` being unset, on the strength of every prior quick task doing so and the standing keep-`master` preference | 2026-09-10 | 29a945b | [260910-r46-bump-usbee-version-to-2-8-0-and-deploy-l](./quick/260910-r46-bump-usbee-version-to-2-8-0-and-deploy-l/) |
| 260915-i4w | The filter switches now also live in the popover, under a collapsible **Options** submenu of `PopupSwitchMenuItem`s two-way bound to the same GSettings keys `prefs.js` binds -- changing one surface moves the other live. Adds a fourth filter, **`hide-builtin-devices`** (`b`, default **false**), in both surfaces. It keys off the daemon's own `mount == 'fixed'`, a fact **verified at the usbeehive source** (`src/summary.rs:645` pushes `mount` only for the literals `fixed`/`removable`, dropping `unknown`/`""` per `src/usb.rs:71`) rather than assumed from the upstream project's `removable == 'fixed'` -- so an **absent** `mount` means "the kernel did not say" and is never treated as built-in, pinned by a test, as is the case-sensitivity of the token. Default `false` chosen so nothing vanishes from an existing list on upgrade, and because a built-in device is a *real device* unlike a hub, which is why `show-hubs` defaults the other way. A built-in device **carrying an issue stays visible** -- the same escape hatch `show-hubs` uses (260905-b0s) -- so the header can never count an issue the list will not show. Predicate put in the **zero-import** `link-verdict.js` so bare-gjs CI can unit-test it rather than only guard it structurally. The two-way binding carries a **re-entrancy latch** instead of resting on `setToggleState()` not emitting `toggled`: GNOME Shell 50.1's UI JavaScript is **not extractable from any installed gresource** here (`gresource list` over `/usr/bin/gnome-shell` and every `/usr/share/gnome-shell/*.gresource` finds no `ui/popupMenu.js`), so that documented behaviour could not be confirmed locally, and two surfaces bound to one key were not left able to write to each other in a loop. Empty-after-filtering now says **"All devices hidden by the current filters"** rather than falsely claiming nothing is attached. 204 assertions green across all four suites under `rtk proxy`; schema round-tripped in an isolated `GSETTINGS_SCHEMA_DIR` (default `false`, all seven keys listed). **Popover not verifiable without a Shell restart**; the prefs window was deliberately not exercised in-process, because the installed extension is a real directory holding the released v2.8.0, not a symlink to this checkout, so testing there would overwrite the user's install. | 2026-09-15 | 0c9f274 | [260915-i4w-add-in-popover-options-quick-toggles-bou](./quick/260915-i4w-add-in-popover-options-quick-toggles-bou/) |
| 260915-ib9 | Removed the dead **`SnapshotJson`** declaration from USBee's inlined `IFACE_XML` and from the reference copy `dbus-iface.xml`. Proved dead **before** removing, not after: a repo-wide `rtk proxy grep` found four mentions and a second grep for any call-shaped invocation of it found **none**. The two survivors are comments in `property-policy.js` describing the **daemon's** method, which stay accurate because usbeehive is untouched and still exports it -- `busctl` and the usbeehive CLI reach it exactly as before. So that they do not later read as staleness, the comment above `IFACE_XML` now states the relationship explicitly, and records why **`Refresh` and `Diagnose` stay** though equally uncalled: both are declared against a planned call site, `SnapshotJson` was not. Two new tests pin their survival so this removal is not read as licence to strip them too. The **byte-equality invariant** (literal vs on-disk XML, less the doctype) preserved and proved **twice** -- the suite's own assertion, plus an independent normalisation outside it putting both sides at exactly **1464 bytes** with a clean `diff`. No daemon change, per the explicit scope limit. No Shell restart needed: this changes only the XML string a proxy is built from, with no UI surface. | 2026-09-15 | 3be0d09 | [260915-ib9-remove-the-dead-snapshotjson-method-from](./quick/260915-ib9-remove-the-dead-snapshotjson-method-from/) |

---
*State initialized: 2026-05-11 after roadmap creation*
*v2.0 milestone opened: 2026-05-14*

## Decisions

- [Phase 04]: Quick task 260910-r46: released 2.8.0 locally (metadata 13 / 2.8.0, changelog rolled, commit 29a945b, zip 25 files / 240,775 B with 15 src/ entries, installed on disk). MINOR bump because the Unreleased section carried Added entries. Deliberately stopped before tag and push; Shell restart left to the user.
- [Phase 04]: Quick task 260915-i4w: new GSettings key `hide-builtin-devices` defaults to **false**. Rationale: a default that hides devices silently omits hardware on first run and on upgrade from 2.8.0, and a built-in device is a real device unlike a hub (which is why `show-hubs` defaults to hiding). Filter keys off `mount == 'fixed'`, verified against usbeehive `src/summary.rs:645` / `src/usb.rs:71` rather than inherited from the upstream project's `removable == 'fixed'`; an absent `mount` is never treated as built-in. Built-in devices carrying an issue stay visible, matching the `show-hubs` escape hatch from 260905-b0s, so the header's issue count cannot name an unreachable row.
- [Phase 04]: Quick task 260915-i4w: the popover/prefs two-way binding carries an explicit re-entrancy latch. GNOME Shell 50.1's `ui/popupMenu.js` is not extractable from any installed gresource on this machine, so the documented "`setToggleState()` does not emit `toggled`" behaviour could not be confirmed locally; the latch makes the binding correct under either semantics rather than leaving a write-loop between two bound surfaces resting on an unverified internal.
- [Phase 04]: Quick task 260915-ib9: `SnapshotJson` dropped from USBee's interface declaration only. usbeehive is untouched and still exports the method, so the `property-policy.js` tier comments that call wire-only keys "reachable through the daemon's SnapshotJson" remain true. `Refresh` and `Diagnose` are equally uncalled but kept, because both are declared against a planned call site; tests now pin their survival so this removal is not generalised.
