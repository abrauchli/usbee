---
quick_id: 260514-g4x
slug: for-the-bubble-tile-icon-use-drive-hardd
description: For the bubble-tile icon use drive-harddisk-usb-symbolic instead of the shipped one
date: 2026-05-14
---

# Quick Task 260514-g4x: Switch bubble-tile icon to drive-harddisk-usb-symbolic

## Goal

Replace the shipped `usb-symbolic.svg` file icon with the system theme icon
`drive-harddisk-usb-symbolic` in the Quick Settings tile.

## Tasks

### Task 1: Update tile.js to use system icon name

**File:** `usbee@bitcreed.us/src/tile.js`

**Action:**
- Remove the `usbGicon` variable and its `Gio.icon_new_for_string(...)` call (lines 27–33)
- Remove the comment block explaining the shipped SVG approach
- Replace `gicon: usbGicon` in the `super({...})` call with `iconName: 'drive-harddisk-usb-symbolic'`

**Done when:** `tile.js` no longer references `usb-symbolic.svg` and uses
`iconName: 'drive-harddisk-usb-symbolic'` for the toggle widget.
