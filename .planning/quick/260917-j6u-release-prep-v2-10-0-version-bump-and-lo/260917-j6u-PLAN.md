---
phase: 260917-j6u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - usbee@bitcreed.us/metadata.json
  - CHANGELOG.md
autonomous: true
requirements: [QUICK-260917-j6u]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`usbee@bitcreed.us/metadata.json` reads `\"version\": 15` and `\"version-name\": \"2.10.0\"`, and no other byte of that file changed."
    - "`CHANGELOG.md` carries a `## [2.10.0] — 2026-09-17` section immediately below an `## [Unreleased]` heading that is still present and still empty."
    - "The new section is narrative, user-facing prose in the same voice as the `[2.9.0]` section — not terse developer bullets — and covers both shipped changes: the capability-named tile title (260917-hkf) and the verdict-coloured speed rating (260917-i43)."
    - "The colour-contrast limitation is stated in one or two plain sentences aimed at a user, with no ratio numbers and no standard cited."
    - "A `[2.10.0]:` reference link exists at the bottom of `CHANGELOG.md` in the file's existing `releases/tag/vX.Y.Z` form, above the `[2.9.0]:` line."
    - "The release commit's subject is exactly `chore: release v2.10.0` and it contains exactly two files."
    - "The packed zip contains entries under the `src/` prefix and its embedded `metadata.json` reads 15 / 2.10.0."
    - "The installed extension directory on disk reads 15 / 2.10.0."
    - "No `v2.10*` tag exists anywhere and nothing was pushed — `origin/master` is byte-identical to where it started."
    - "This repo's own extension sources under `usbee@bitcreed.us/` survive the install untouched."
  artifacts:
    - "usbee@bitcreed.us/metadata.json — version 15, version-name 2.10.0"
    - "CHANGELOG.md — new `## [2.10.0] — 2026-09-17` section plus its reference link"
    - "usbee@bitcreed.us.shell-extension.zip — repo root, gitignored, never staged"
    - "~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/ — still a real directory, now holding 2.10.0"
  key_links:
    - "metadata.json `version-name` -> the CHANGELOG heading -> the tag the user will later cut: `.github/workflows/release.yml` asserts `version-name` == tag, so a mismatch here fails that workflow later, not now"
    - "`--extra-source=src` -> the zip's `src/` entries: a wrong path exits 0 and prints nothing, so only inspecting the zip catches it"
    - "the symlink guard -> `gnome-extensions install --force`: that guard is the only thing standing between `--force` and deletion of this repo's extension sources (260910-hrj)"
    - "the packed zip -> the on-disk installed `metadata.json`: the only honest proof of the install, since `gnome-extensions info` reports stale in-process metadata until the Shell restarts"
---

<objective>
Cut v2.10.0 locally: bump `metadata.json`, write the release's CHANGELOG
section, commit, pack the zip, and install it for this user — stopping
deliberately before the tag and the push.

Purpose: two user-visible features have landed on `master` since v2.9.0 and
are not yet packaged or written down. A release that ends at a local install
lets the work be used and reviewed while leaving the GitHub Release the user's
to trigger.

Output: a `chore: release v2.10.0` commit touching exactly two files, a
verified `.shell-extension.zip`, and an installed 2.10.0 on disk.

There is no tracer slice to lead with: this plan creates no new code path.
The three tasks *are* the release pipeline end-to-end — edit, pack, install —
and Task C's on-disk read-back is the end-to-end proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CHANGELOG.md
@usbee@bitcreed.us/metadata.json
@.planning/quick/260917-hkf-tile-title-shows-max-link-capability-not/260917-hkf-SUMMARY.md
@.planning/quick/260917-i43-colour-code-the-displayed-speed-rating-b/260917-i43-SUMMARY.md
</context>

<hard_constraints>

- **No tag. No push. Not at any point, for any reason.** Pushing a `v2.10.0`
  tag fires `.github/workflows/release.yml`, which builds and publishes a
  public GitHub Release. That is explicitly out of scope and is the user's to
  trigger. Do not run `git tag`, `git push`, or `gh release` in this task.
- **No GNOME Shell restart, no re-login prompt, no `pkill -f gnome-shell`.**
  The user restarts on their own schedule.
- **Expected non-failure, do not "fix" it:** after the install,
  `gnome-extensions info usbee@bitcreed.us` still reports 2.9.0. It reads live
  in-process metadata, not disk, and keeps doing so until the Shell restarts.
  This is documented behaviour in CLAUDE.md. It is not a failed install; do not
  reinstall, do not repack, do not restart anything to chase it. Read the
  on-disk file instead, which is what Task C does.
- **rtk filters command output.** Any check whose correctness depends on raw
  output — the `unzip -l` listing, the symlink test, every version grep, the
  `git` porcelain reads — MUST run under `rtk proxy`. An unproxied check can
  succeed vacuously against stripped output.
- **Do not regenerate `po/usbee@bitcreed.us.pot`.** No user-visible string
  changes in this task (260917-hkf already regenerated it when its strings
  changed). And never pass `--package-version` anywhere: releases bump
  `metadata.json` and nothing else version-shaped.
- **Branch is `master`.** Keep it. Never rename it, never suggest renaming it.
- **The release commit stages exactly two files** — `usbee@bitcreed.us/metadata.json`
  and `CHANGELOG.md` — by explicit path. `.gsd/` and `.planning/quick-batches/`
  are pre-existing untracked directories and stay untracked. This task's own
  planning documents are committed separately, never folded into the release
  commit.
- **The built zip is gitignored and must never be staged or committed.**
- **Abort conditions, both hard:** if the packed zip has no `src/` entries,
  stop the whole task and report — do not install. If the install destination
  is a symlink, stop and report — do not install.
- **The human is unavailable.** Decide from CLAUDE.md and the facts recorded
  below; flag any inference for later audit. Escalate only for a genuinely
  irreversible fork.

</hard_constraints>

<inferred_decisions>

Recorded for later audit — the human is unavailable, so each of these was
settled from CLAUDE.md, the repo's own precedent, and the verified facts.

- **D-1 — MINOR bump, 2.9.0 → 2.10.0, `version` 14 → 15.** Two user-facing
  behaviour changes, no user-facing break, no API removed. `version` is a
  monotonic integer unrelated to semver (CLAUDE.md), so it goes to 15
  regardless of the semver shape.
- **D-2 — Both entries go under a single `### Changed` heading.** Neither
  change adds a surface that did not exist: the tile title and the speed
  rating were both already displayed and now say something different. The
  meaningless `USB 2.1`-style string the old title could produce is narrated
  *inside* its entry rather than split into a separate `### Fixed` — matching
  how the `[2.8.0]` "Changed" entries explain the defect they correct.
- **D-3 — The colour-contrast limitation is disclosed, in user language.** It
  is a real limitation of what shipped, and the `[2.8.0]` and `[2.9.0]`
  sections set the precedent that USBee's changelog says what a feature cannot
  do. It is written as "the colour is closer to the background than it should
  be on some themes, so trust the words beside it" — not as a ratio, not as a
  standard citation.
- **D-4 — Reference link added now, even though it will 404 until the user
  tags.** Same choice 260915-mpf made when it cut 2.9.0 locally. The file has
  **no `Unreleased` compare link** and one is again deliberately not invented.
- **D-5 — The test suites are not re-run.** This task changes no JavaScript;
  no test reads `metadata.json` (only `release.yml` does, and only at tag
  time). Task A's own verification proves the release commit touches exactly
  two non-JS files, which is what makes skipping the suites safe rather than
  lazy. The 687-assertion green baseline from `9bdb485` therefore still stands
  for the tree being packed.
- **D-6 — Committed straight to `master` with `git.allow_default_branch_commits`
  still unset**, as every prior quick task in this repo has done. Flagged, as
  every prior one flagged it.

</inferred_decisions>

<changelog_source_material>

The intended content of the new section. The executor may adjust wording for
flow, but must not change the facts, must not add version numbers, ratios or
standard names, and must keep both entries under one `### Changed` heading.
Where a phrasing feels uncertain, check it against the two SUMMARY files in
`<context>` rather than softening it.

Heading line, exactly (em dash U+2014, same as every other section heading):

`## [2.10.0] — 2026-09-17`

Then `### Changed`, then these two entries:

**Entry 1 — the tile title (260917-hkf).** The tile now names what a device's
link can actually do instead of the version number printed in its descriptor:
`USB 5Gbps`, `USB 10Gbps`, `USB 480Mbps` — the plain-rate names the USB-IF
itself now asks products to use — in place of a `USB 2.1`-style string, which
named nothing anyone could act on and for some devices was not a real version
at all. Which device the tile speaks for is now decided by the fastest link
the machine has rather than by the highest descriptor number, so the title
stops flipping between "USB 2.0", "USB 2.1" and "USB 3.0" as devices come and
go. The line underneath says what is actually happening on that link: *full
capability* when the device is running as fast as it can, and the measured
rate — *linked at 480 Mb/s* — when it is not.

**Entry 2 — the coloured speed rating (260917-i43).** The speed rating is now
coloured by the verdict the daemon reports for the link: green when the device
is running at its full capability, orange when it is linked below what it can
do, red when the link is degraded. The same colour appears in all three places
the rating is shown — the tile, the device's row in the list, and the Link line
in its detail panel — so a device cannot read one way collapsed and another way
expanded. A device that says nothing about its own capability is left
uncoloured rather than guessed at, which today is most of them: only devices
carrying a BOS descriptor report a verdict at all. The colour never replaces a
word — every string that was there before is unchanged. One caveat: on a light
theme the green and the orange sit closer to the background than they should,
and on a dark theme the red does, because USBee's styling does not yet vary its
colours by light or dark appearance. Until it does, the words beside the colour
remain the thing to trust.

</changelog_source_material>

<tasks>

<task type="auto">
  <name>Task A: Bump the version, write the 2.10.0 section, commit</name>
  <files>usbee@bitcreed.us/metadata.json, CHANGELOG.md</files>
  <read_first>CHANGELOG.md lines 1-90 (the `[Unreleased]` heading, the `[2.9.0]` section whose voice is the target) and lines 735-754 (the reference-link block); usbee@bitcreed.us/metadata.json in full</read_first>
  <action>
Edit `usbee@bitcreed.us/metadata.json` with two scoped replacements: the
`"version"` value 14 becomes 15, and the `"version-name"` value "2.9.0" becomes
"2.10.0" (D-1). Touch nothing else in that file — not the description, not
`shell-version`, not the key order, not the trailing newline. Use Edit, never
Write, on both files in this task.

Then edit `CHANGELOG.md`. Anchor on the heading strings, not on line numbers.
Insert the new section between the `## [Unreleased]` heading and the
`## [2.9.0]` heading, so that `## [Unreleased]` survives above it and stays
empty — exactly one blank line separating each heading from what follows, the
same spacing every other section in the file uses. The section's heading,
subheading and body come from `changelog_source_material` above: one
`### Changed` heading (D-2), two narrative entries, the colour caveat inside
the second entry (D-3). Voice is the acceptance bar here, not an aside — the
target is the `[2.9.0]` section's register: prose written for someone deciding
whether to upgrade, explaining what changed and why it matters. Terse
developer bullets fail this task even when factually correct.

Then add the reference link (D-4). Read the existing block at the bottom of
the file first and match it byte-for-byte in shape; the links descend by
version, so the new one goes immediately above the 2.9.0 line. Add no compare
link for the unreleased section — the file has never had one and inventing one
is out of scope.

Stage the two files by explicit path and commit with the subject line exactly
`chore: release v2.10.0`, plus the standard `Co-Authored-By: Claude Opus 5
&lt;noreply@anthropic.com&gt;` trailer. No quick-task scope on the release commit
subject — CLAUDE.md's documented convention is the bare form. Do not stage
`.gsd/`, `.planning/quick-batches/`, this task's planning documents, or
anything else.
  </action>
  <verify>
    <automated>rtk proxy sh -c 'cd /home/blk/projects/rust/usbee &amp;&amp; git log -1 --pretty=%s | grep -qx "chore: release v2.10.0" &amp;&amp; test "$(git show --name-only --pretty=format: HEAD | grep -c .)" -eq 2 &amp;&amp; git diff --numstat HEAD~1 HEAD -- "usbee@bitcreed.us/metadata.json" | grep -qx "2	2	usbee@bitcreed.us/metadata.json" &amp;&amp; test "$(git diff --numstat HEAD~1 HEAD -- CHANGELOG.md | cut -f2)" -eq 0 &amp;&amp; grep -q "\"version\": 15," usbee@bitcreed.us/metadata.json &amp;&amp; grep -q "\"version-name\": \"2.10.0\"" usbee@bitcreed.us/metadata.json &amp;&amp; test "$(grep -c "^\[2\." CHANGELOG.md)" -eq 15 &amp;&amp; grep -qx "\[2.10.0\]: https://github.com/abrauchli/usbee/releases/tag/v2.10.0" CHANGELOG.md &amp;&amp; grep -n "^## \[" CHANGELOG.md | head -3 | tr "\n" " " | grep -q "Unreleased.*2.10.0.*2.9.0" &amp;&amp; awk "/^## \[Unreleased\]/{f=1;next} /^## \[/{f=0} f&amp;&amp;NF{c++} END{exit c?1:0}" CHANGELOG.md &amp;&amp; echo TASK_A_OK'</automated>
    <human-check>Read the new section start to finish against the `[2.9.0]` section directly above it. It passes only if the two are indistinguishable in register — full sentences explaining the change and why it matters, no bare feature labels, no ratio numbers, no standard cited in the colour caveat.</human-check>
  </verify>
  <done>HEAD's subject is exactly `chore: release v2.10.0`; it contains exactly two files; `metadata.json` changed exactly 2 lines for 2 lines and now reads 15 / 2.10.0; the `CHANGELOG.md` diff is purely additive (zero deletions); the three newest `## [` headings are Unreleased, 2.10.0, 2.9.0 in that order; the Unreleased section is empty; there are now 15 version reference links, the newest being the 2.10.0 one.</done>
</task>

<task type="auto">
  <name>Task B: Pack the zip and prove it before it goes anywhere near the Shell</name>
  <files>usbee@bitcreed.us.shell-extension.zip (gitignored build output, repo root)</files>
  <action>
Run the pack from the repo root `/home/blk/projects/rust/usbee`, with the
command text after the working directory byte-identical to CLAUDE.md's
recorded invocation: `gnome-extensions pack usbee@bitcreed.us --podir=../po
--extra-source=src --extra-source=icons --force`. Confirm the effective working
directory is the repo root in the same invocation — `--podir` resolves relative
to the source directory and the zip is written to the working directory, so
running it from anywhere else quietly produces a different artifact. Do not
substitute absolute paths into the command's own arguments; establish the
directory, then run it verbatim.

Then prove the artifact before trusting it. A wrong `--extra-source` path makes
pack exit 0 and print nothing, so a zip that lost every module looks exactly
like a good one from the command's output alone. Inspect the zip's listing
under `rtk proxy` and check that entries under the `src/` prefix are present at
all — that prefix appearing is the real signal, not the total. For reference:
a correct zip is around 25 entries and roughly 231 KB; the failure signature is
about 10 entries and 29 KB. Also read the `metadata.json` out of the zip itself
and confirm it carries the bump, which proves the zip was built from the
committed tree rather than a stale one.

If the `src/` entries are absent, ABORT the entire task here. Report the
listing and stop. Do not install a zip that failed this gate, do not repack
speculatively, and do not proceed to Task C.

No test run is needed before packing (D-5): this task changed no JavaScript,
which Task A's two-file diff already proved.
  </action>
  <verify>
    <automated>rtk proxy sh -c 'cd /home/blk/projects/rust/usbee &amp;&amp; test "$(unzip -l usbee@bitcreed.us.shell-extension.zip | grep -c " src/")" -ge 10 &amp;&amp; unzip -p usbee@bitcreed.us.shell-extension.zip metadata.json | grep -q "\"version\": 15," &amp;&amp; unzip -p usbee@bitcreed.us.shell-extension.zip metadata.json | grep -q "\"version-name\": \"2.10.0\"" &amp;&amp; unzip -l usbee@bitcreed.us.shell-extension.zip | tail -2 &amp;&amp; echo TASK_B_OK'</automated>
  </verify>
  <done>`usbee@bitcreed.us.shell-extension.zip` exists at the repo root, lists at least 10 entries under the `src/` prefix, and the `metadata.json` inside it reads 15 / 2.10.0. The printed entry/byte totals are recorded in the summary for comparison against the ~25-entry / ~231 KB reference.</done>
</task>

<task type="auto">
  <name>Task C: Symlink guard, install, verify what actually landed on disk</name>
  <files>~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/ (install destination, outside the repo)</files>
  <precondition>`usbee@bitcreed.us.shell-extension.zip` exists at the repo root and passed Task B's `src/` gate; if Task B aborted, this task does not run at all.</precondition>
  <action>
Immediately before installing — not earlier in the task, not carried over from
a reading taken at planning time — re-run the symlink guard under `rtk proxy`:
test whether `~/.local/share/gnome-shell/extensions/usbee@bitcreed.us` is a
symlink. It is expected to be a real directory. If it reports SYMLINK, STOP and
report; do not install. Per 260910-hrj, `install --force` through a symlink
deletes the contents of the symlink's *target* — which in the symlinked-dev
setup is this repo's own extension sources, every module under `src/`,
`metadata.json`, `schemas/`, `icons/`, the lot. The guard runs under
`rtk proxy` because the rtk hook strips the type and permission field from a
bare `ls -ld`, which would make the check pass vacuously on a symlink.

With the guard green, run `gnome-extensions install --force
usbee@bitcreed.us.shell-extension.zip` from the repo root. It compiles the
GSettings schemas itself; no separate `glib-compile-schemas` run is wanted.

Then verify what landed, by reading the installed `metadata.json` on disk under
`rtk proxy` and expecting 15 / 2.10.0. Do not use `gnome-extensions info` for
this — see the expected-non-failure note in `hard_constraints`; it will report
2.9.0 and that is correct behaviour, not a failed install. Confirm this repo's
own extension sources survived the install untouched, which is the other half
of the 260910-hrj guard: a clean `git status` over `usbee@bitcreed.us/` proves
nothing was deleted through a path that was not supposed to be a symlink.

Finally, confirm the release stopped where it was meant to: no `v2.10*` tag
exists, `origin/master` is exactly where it started, and the built zip is
ignored rather than staged. The working tree should be clean apart from the
pre-existing untracked `.gsd/` and `.planning/quick-batches/` and this task's
own planning directory.

Do not restart the Shell and do not ask the user to. Record in the summary that
a restart is outstanding before 2.10.0 actually loads, and that it is now
accumulated across several prior tasks.
  </action>
  <verify>
    <automated>rtk proxy sh -c 'cd /home/blk/projects/rust/usbee &amp;&amp; D="$HOME/.local/share/gnome-shell/extensions/usbee@bitcreed.us" &amp;&amp; test ! -L "$D" &amp;&amp; test -d "$D" &amp;&amp; grep -q "\"version\": 15," "$D/metadata.json" &amp;&amp; grep -q "\"version-name\": \"2.10.0\"" "$D/metadata.json" &amp;&amp; test -z "$(git status --porcelain -- "usbee@bitcreed.us")" &amp;&amp; test -z "$(git tag -l "v2.10*")" &amp;&amp; test -z "$(git ls-remote --tags origin "v2.10*")" &amp;&amp; git log --oneline origin/master..HEAD | grep -q "chore: release v2.10.0" &amp;&amp; git check-ignore -q usbee@bitcreed.us.shell-extension.zip &amp;&amp; echo TASK_C_OK'</automated>
    <human-check>Confirm from the run log that no `git push`, `git tag` or `gh release` command was issued at any point in this task — the assertions above prove the end state, and the log proves the path taken to it.</human-check>
  </verify>
  <done>The destination is still a real directory; its `metadata.json` reads 15 / 2.10.0; `usbee@bitcreed.us/` in this repo is clean; no `v2.10*` tag exists locally or on the remote; `origin/master` is unmoved; the zip is gitignored and unstaged. `gnome-extensions info` reporting 2.9.0 is noted as expected and left alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → packed zip | `gnome-extensions pack` selects which files become the artifact; a silently wrong selection ships a broken extension |
| packed zip → `~/.local/share/gnome-shell/extensions/` | `install --force` writes into a path the live Shell loads and executes, and deletes what is there first |
| local `master` → `origin` + tags | a pushed tag triggers `release.yml`, which publishes a public GitHub Release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260917-j6u-01 | Tampering | `gnome-extensions install --force` on the destination path | high | mitigate | Mandatory symlink guard under `rtk proxy` immediately before install (Task C); hard abort on SYMLINK; post-install `git status` over `usbee@bitcreed.us/` proves the repo's sources survived |
| T-260917-j6u-02 | Tampering | `gnome-extensions pack --extra-source=src` | medium | mitigate | Pre-install zip inspection under `rtk proxy` requiring `src/` entries and the bumped embedded `metadata.json`; hard abort before install if absent (Task B) |
| T-260917-j6u-03 | Elevation of Privilege | `git push` / `git tag` → `.github/workflows/release.yml` | high | mitigate | Explicit prohibition in `hard_constraints`; Task C asserts no `v2.10*` tag exists locally or on the remote and that `origin/master` is unmoved |
| T-260917-j6u-04 | Information Disclosure | `CHANGELOG.md` prose written into a public file | low | mitigate | Content is drawn only from already-public commits and the two SUMMARY files; no paths, hostnames, device serials or environment details go into the section |
| T-260917-j6u-05 | Spoofing | verification reading filtered rather than raw command output | medium | mitigate | Every correctness-bearing check runs under `rtk proxy`; the rtk hook strips `ls -ld` type bits and test-result lines, which would make the symlink and version gates pass vacuously |
| T-260917-j6u-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this task. `gnome-extensions pack` is a GNOME-shipped tool operating solely on files already in this repo; nothing is fetched from a registry, so no package-legitimacy audit applies |
</threat_model>

<verification>

1. `rtk proxy git show --stat HEAD` — subject exactly `chore: release v2.10.0`,
   exactly two files, `CHANGELOG.md` with zero deletions and
   `usbee@bitcreed.us/metadata.json` at 2 insertions / 2 deletions. The diff
   shape is the guard, not a predicted line count: 260915-mpf learned that a
   file viewer's display numbering and `wc -l` disagree by one here.
2. `rtk proxy grep '"version' usbee@bitcreed.us/metadata.json` — 15 and 2.10.0.
3. The three newest `## [` headings are Unreleased, 2.10.0, 2.9.0; the
   Unreleased section has no non-blank content.
4. 15 version reference links at the bottom, the new one matching the existing
   `releases/tag/vX.Y.Z` form; no compare link invented for the unreleased
   section.
5. The prose reads in the `[2.9.0]` voice and discloses the colour-contrast
   limitation in one or two user-facing sentences, with no ratios and no
   standard named.
6. `rtk proxy unzip -l usbee@bitcreed.us.shell-extension.zip` — `src/` entries
   present (the real gate); totals recorded against the ~25-entry / ~231 KB
   reference; the embedded `metadata.json` reads 15 / 2.10.0.
7. The symlink guard was re-run immediately before the install and reported a
   real directory.
8. The installed on-disk `metadata.json` reads 15 / 2.10.0 — read directly, not
   via `gnome-extensions info`, which will still say 2.9.0 and is expected to.
9. `usbee@bitcreed.us/` in this repo is clean after the install.
10. No `v2.10*` tag locally or on the remote; `origin/master` unmoved; the zip
    ignored and unstaged; the tree otherwise clean apart from the pre-existing
    untracked `.gsd/` and `.planning/quick-batches/`.

</verification>

<success_criteria>

- v2.10.0 is cut and installed locally, and a GNOME Shell restart is the only
  thing standing between the user and running it.
- The CHANGELOG section could be handed to a stranger deciding whether to
  upgrade and would answer them — including the one thing that is not yet
  right about the colours.
- Nothing was published. No tag exists, nothing was pushed, and the GitHub
  Release remains entirely the user's to trigger.
- Every gate that protects this repo from `install --force` was run against
  live state at the moment it mattered, not inherited from an earlier reading.

</success_criteria>

<output>
Create `.planning/quick/260917-j6u-release-prep-v2-10-0-version-bump-and-lo/260917-j6u-SUMMARY.md`
when done. Record: the zip's actual entry count and byte total, the symlink
guard's live reading, the on-disk version read-back, the confirmation that no
tag and no push happened, and that a Shell restart is outstanding (now
accumulated across 260915-i4w, 260915-ikd, 260915-mpf, 260916-2qs, 260917-hkf,
260917-i43 and this task). Flag D-1 through D-6 as inferences made while the
human was unavailable.
</output>
</content>
</invoke>
