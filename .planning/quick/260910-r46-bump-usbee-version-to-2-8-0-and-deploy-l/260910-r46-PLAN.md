---
phase: 260910-r46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - usbee@bitcreed.us/metadata.json
  - CHANGELOG.md
autonomous: true
requirements: [QUICK-260910-r46]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Repo `usbee@bitcreed.us/metadata.json` reads `\"version\": 13` and `\"version-name\": \"2.8.0\"`."
    - "CHANGELOG.md carries `## [2.8.0] — 2026-09-10` holding the former Unreleased content verbatim, with a fresh EMPTY `## [Unreleased]` heading above it."
    - "CHANGELOG.md has a `[2.8.0]:` reference link in the bottom block, matching the existing link style."
    - "A single commit `chore: release v2.8.0` contains exactly those two files."
    - "The packed zip contains entries under the `src/` prefix (not the 10-entry / ~29 KB broken variant)."
    - "The INSTALLED extension at `~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/metadata.json` reports 13 / 2.8.0."
    - "No `v2.8.0` git tag exists and nothing was pushed — this release deliberately stops at a local install."
  artifacts:
    - usbee@bitcreed.us/metadata.json
    - CHANGELOG.md
    - usbee@bitcreed.us.shell-extension.zip
    - ~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/metadata.json
  key_links:
    - "`--extra-source=src` in the pack invocation → `src/` entries in the zip → a working installed extension. This link fails SILENTLY (pack exits 0, prints nothing), so it must be checked, never assumed."
    - "`version-name` in the repo metadata.json → `version-name` in the installed metadata.json. This is the only proof the zip that was built is the zip that landed."
---

<objective>
Cut USBee 2.8.0 locally: bump the version fields, roll the populated
`## [Unreleased]` changelog section into a dated `## [2.8.0]` section,
commit, pack, and install — stopping deliberately before tag and push.

Purpose: get the accumulated unreleased work (daemon-state probe +
Start button, Link/Capability split, withdrawal of the faster-port
remedy, alt-mode restatement) onto the local machine under a real
version number, without publishing a GitHub Release.

Output: a `chore: release v2.8.0` commit, a verified
`usbee@bitcreed.us.shell-extension.zip`, and an installed extension
reporting 2.8.0 / 13 on disk.
</objective>

<scope_boundary>
This is steps 1–3 of CLAUDE.md's Release Process, plus the local pack
and install from its "Packing and local install (development)"
subsection. It is **not** steps 4–5.

**Do NOT create the `v2.8.0` git tag. Do NOT push.** Tagging fires
`.github/workflows/release.yml`, which builds the zip and publishes a
GitHub Release. The whole point of this task is to deploy locally
without publishing. Stop at the install.

**Do NOT restart GNOME Shell.** See `<shell_restart>` below.
</scope_boundary>

<tooling_hazard>
An `rtk` hook transparently rewrites shell commands and **strips lines
from command output**. Any check whose correctness depends on seeing
raw output — the `unzip -l` listing in Task 2 and the `grep` in Task 3
— MUST be run as `rtk proxy <cmd>`. Without it a filtered-away line
makes the check succeed vacuously, which is exactly the failure mode
these checks exist to catch.

Every verify command in this plan is already written with the
`rtk proxy` prefix where it matters. Do not strip it.
</tooling_hazard>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CHANGELOG.md
@usbee@bitcreed.us/metadata.json
</context>

<established_facts>
Verified upstream — do NOT re-derive, do NOT re-check:

- Current `metadata.json`: `"version": 12` (line 9), `"version-name": "2.7.1"` (line 10).
- `CHANGELOG.md` is 677 lines. `## [Unreleased]` is at **line 9**; its
  content occupies lines 11–105 (95 lines: Added / Changed / Fixed);
  line 106 is blank; `## [2.7.1] — 2026-09-10` is at **line 107**.
- The reference-link block runs lines **666–677** and begins with
  `[2.7.1]: https://github.com/abrauchli/usbee/releases/tag/v2.7.1`.
  **There is NO `[Unreleased]:` compare link in this file.** Do not add
  one — the file's own style has never had one.
- Version headings use an em dash with spaces: `## [2.7.1] — 2026-09-10`.
- `~/.local/share/gnome-shell/extensions/usbee@bitcreed.us` is a **real
  directory**, not a symlink (confirmed with `ls -ld`). CLAUDE.md's
  symlinked-dev-install hazard — where `install --force` deletes
  through the symlink and wipes the repo's extension sources — does
  **not** apply here, so `install --force` is safe. The read-only
  `ls -ld` pre-flight in Task 3 is a zero-cost re-confirmation of a
  documented foot-gun; it is **not** a licence to `rm -rf` anything.
- Today's date is **2026-09-10**.
- Branch is `master`, worked sequentially (no worktree isolation).
- No source file under `src/` changes in this task, so the zip should
  have the same shape as the last one packed (quick task 260910-p91):
  25 entries, 15 of them under `src/`, ~241 KB uncompressed.
</established_facts>

<tasks>

<task type="auto">
  <name>Task 1: Bump version fields, roll the changelog, commit</name>
  <files>usbee@bitcreed.us/metadata.json, CHANGELOG.md</files>
  <action>
Two scoped edits, then one commit. Use `Edit` for both files — never
`Write`, which would risk clobbering the 677-line changelog.

**metadata.json** — change exactly two values, nothing else:
`"version": 12` becomes `"version": 13`; `"version-name": "2.7.1"`
becomes `"version-name": "2.8.0"`. Leave `shell-version`,
`description`, and every other key byte-identical. A MINOR bump is
correct because the Unreleased section has Added entries, not only
Fixed ones.

**CHANGELOG.md** — roll the section, do not rewrite it. Replace the
single line `## [Unreleased]` at line 9 with these four lines:
an empty `## [Unreleased]` heading, a blank line, the heading
`## [2.8.0] — 2026-09-10`, and a blank line. Use the same em dash
character (U+2014, spaced) the other version headings use. The 95 lines
of Added / Changed / Fixed content that followed must survive
**verbatim** — do not reword, reorder, merge, or re-sort the
subsections, and do not touch any older version section.

Then add one reference link at the **top** of the bottom link block,
immediately above the existing `[2.7.1]:` line, in that block's exact
style: `[2.8.0]: https://github.com/abrauchli/usbee/releases/tag/v2.8.0`.
That URL will 404 until a tag is eventually pushed; that is expected
and matches how every prior version's link was added at release-prep
time. Do not add an `[Unreleased]:` link — the file has never had one.

Stage only those two paths and commit with subject
`chore: release v2.8.0` plus the project's standard commit trailers.
Nothing else goes in this commit.
  </action>
  <verify>
    <automated>rtk proxy grep -n '"version' /home/blk/projects/rust/usbee/usbee@bitcreed.us/metadata.json</automated>
    <automated>rtk proxy python3 -c "import json;d=json.load(open('/home/blk/projects/rust/usbee/usbee@bitcreed.us/metadata.json'));assert d['version']==13 and d['version-name']=='2.8.0', d;print('metadata OK', d['version'], d['version-name'])"</automated>
    <automated>rtk proxy sed -n '9,13p' /home/blk/projects/rust/usbee/CHANGELOG.md</automated>
    <automated>rtk proxy grep -n '^## \[' /home/blk/projects/rust/usbee/CHANGELOG.md | head -4</automated>
    <automated>rtk proxy grep -n 'releases/tag/v2\.8\.0' /home/blk/projects/rust/usbee/CHANGELOG.md</automated>
    <automated>rtk proxy wc -l /home/blk/projects/rust/usbee/CHANGELOG.md</automated>
    <automated>rtk proxy git -C /home/blk/projects/rust/usbee show --stat --oneline HEAD</automated>
  </verify>
  <done>
- `grep '"version'` prints `"version": 13,` and `"version-name": "2.8.0"`.
- `sed -n '9,13p'` shows: `## [Unreleased]`, blank, `## [2.8.0] — 2026-09-10`, blank, `### Added` — i.e. the Unreleased section is now genuinely empty and the content moved down under the dated heading.
- The first four `^## [` headings are, in order: `## [Unreleased]`, `## [2.8.0] — 2026-09-10`, `## [2.7.1] — 2026-09-10`, `## [2.7.0] — 2026-09-05`.
- The `[2.8.0]:` reference link exists and sits immediately above `[2.7.1]:`.
- `wc -l` reports **680** lines: 677 + 2 (the new empty Unreleased heading and its blank line) + 1 (the new reference link). A different number means content was lost or duplicated — diff against `HEAD~1` before proceeding rather than guessing.
- `git show --stat` lists exactly two files, subject `chore: release v2.8.0`, and no tag was created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pack, and prove the zip is not the broken variant</name>
  <precondition>The `gnome-extensions` CLI is on PATH (`command -v gnome-extensions`); it ships with gnome-shell. If absent, halt — nothing in this plan can proceed.</precondition>
  <files>usbee@bitcreed.us.shell-extension.zip</files>
  <action>
Run the pack invocation **from the repo root**
`/home/blk/projects/rust/usbee` — `--podir=../po` is resolved relative
to the extension directory, so a different cwd silently packs the wrong
thing or fails. The Bash tool resets cwd between calls; if the shell is
not already at the repo root, change to it first.

Use exactly the invocation CLAUDE.md's Release Process pins as shared
source of truth with `release.yml` — do not shorten it, and in
particular do not drop either `--extra-source`:

    gnome-extensions pack usbee@bitcreed.us --podir=../po --extra-source=src --extra-source=icons --force

Then inspect the result with `unzip -l` under `rtk proxy`. This step
exists because **a wrong `--extra-source` path is silently ignored**:
pack exits 0 and prints nothing, so a zip missing every module under
`src/` is indistinguishable from a good one at the CLI. The gate is the
presence of the `src/` prefix in the listing. Expect 25 entries with 15
under `src/` (~241 KB uncompressed, unchanged from the last pack since
no source file moved in this task); the broken signature is ~10 entries
/ ~29 KB with no `src/` at all. Treat the exact totals as
informational — the entry count grows with every new module — and the
`src/` prefix appearing at all as the real signal.

Record the ACTUAL entry count and byte total in the summary.

**If `src/` is absent from the listing: STOP. Do not install.** Report
the failure with the actual listing; installing a `src/`-less zip over
a working extension breaks it.
  </action>
  <verify>
    <automated>rtk proxy unzip -l /home/blk/projects/rust/usbee/usbee@bitcreed.us.shell-extension.zip</automated>
    <automated>rtk proxy sh -c "unzip -l /home/blk/projects/rust/usbee/usbee@bitcreed.us.shell-extension.zip | grep -c ' src/'"</automated>
  </verify>
  <done>
- The `unzip -l` listing contains entries under the `src/` prefix, and the `src/` count is at least 14 (expected 15).
- The total entry count and uncompressed byte total are recorded verbatim in the summary (expected ~25 entries / ~241 KB; anything near 10 entries / ~29 KB is the broken variant and is a STOP).
  </done>
</task>

<task type="auto">
  <name>Task 3: Install, and verify what actually landed on disk</name>
  <precondition>`ls -ld ~/.local/share/gnome-shell/extensions/usbee@bitcreed.us` shows a real directory (mode starts `d`), not a symlink (`l`). Established upstream; this is a read-only re-confirmation. If it unexpectedly shows a symlink, halt — do NOT `rm -rf` it and do NOT run `install --force`.</precondition>
  <files>~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/metadata.json</files>
  <action>
Install the zip built in Task 2 for the current user:

    gnome-extensions install --force usbee@bitcreed.us.shell-extension.zip

`install` compiles `schemas/gschemas.compiled` itself — do not run
`glib-compile-schemas` separately.

Then verify what landed **on disk**, under `rtk proxy` so no line is
filtered away:

    grep '"version' ~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/metadata.json

Do **not** verify with `gnome-extensions info`. That command reads live
in-process metadata and keeps reporting the previously-loaded version
until the Shell restarts, so a stale version there is expected and
proves nothing either way. The on-disk `grep` is the only honest check
available mid-session.
  </action>
  <verify>
    <automated>rtk proxy ls -ld /home/blk/.local/share/gnome-shell/extensions/usbee@bitcreed.us</automated>
    <automated>rtk proxy grep '"version' /home/blk/.local/share/gnome-shell/extensions/usbee@bitcreed.us/metadata.json</automated>
    <automated>rtk proxy git -C /home/blk/projects/rust/usbee status --short</automated>
  </verify>
  <done>
- The final on-disk verification prints exactly these two lines (order as in the file):

      "version": 13,
      "version-name": "2.8.0"

  Any other value — most likely `12` / `2.7.1` — means the old zip was installed or the install did not take, and is a failure.
- `ls -ld` still shows a directory, and `git status --short` shows the repo's `usbee@bitcreed.us/` sources intact (no mass deletions). This is the documented symlink foot-gun's blast-radius check.
- No `v2.8.0` tag was created; nothing was pushed.
  </done>
</task>

</tasks>

<shell_restart>
**A GNOME Shell restart is required for the new code to actually load**
— Xorg: `Alt+F2` → `r`; Wayland: full log out and log back in.

**The executor must NOT attempt this.** Restarting the Shell on Wayland
terminates the session (and with it this agent); on Xorg it disrupts
the user's desktop without warning. The install is complete and correct
with the old code still running in memory. Simply state in the summary
that 2.8.0 is on disk and goes live at the user's next login.

Corollary: nothing in this plan can be verified by observing the live
popover or tile. Do not try. The preferences window *is* separately
testable mid-session (it runs in its own `org.gnome.Shell.Extensions`
process) but exercising it is **out of scope** for this task.
</shell_restart>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → `~/.local/share/gnome-shell/extensions/` | `gnome-extensions install --force` writes into the user's live extension directory; a destructive delete precedes the unpack. |
| build output → live Shell process | The packed zip becomes code that runs inside `gnome-shell` at next login. A malformed zip degrades the desktop, not just this task. |

No network egress, no credentials, no package-manager installs, and no
untrusted input are in scope. This task edits two text files in the
repo and runs two local CLI commands.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r46-01 | Tampering | `gnome-extensions install --force` vs. the install destination | high | mitigate | `--force` deletes *through* a symlinked dev install and wiped this repo's `usbee@bitcreed.us/` sources once already (quick task 260910-hrj). Destination confirmed a real directory upstream; Task 3 re-asserts it read-only with `ls -ld` before installing and halts on a symlink, and its `git status --short` check bounds the blast radius afterwards. |
| T-r46-02 | Tampering | zip contents (`--extra-source=src`) | medium | mitigate | A wrong `--extra-source` is silently ignored — pack exits 0 and prints nothing, producing a `src/`-less zip that looks identical to a good one. Task 2 gates the install on `unzip -l` showing the `src/` prefix, run under `rtk proxy` so the listing cannot be filtered away, with an explicit STOP. |
| T-r46-03 | Tampering | `rtk` output filtering on verification commands | medium | mitigate | The hook strips lines from command output, so an unwrapped `grep`/`unzip -l` check can pass vacuously — a false "verified". Every output-dependent command in this plan is prefixed `rtk proxy`. |
| T-r46-04 | Elevation of Privilege | installed extension code | low | accept | Code installed is this repo's own source, unchanged in this task (only `metadata.json` differs). It runs with the user's own privileges in their own Shell, as it already does today. No new trust is extended. |
| T-r46-05 | Information Disclosure | commit / release artifacts | low | accept | Nothing leaves the machine: no tag, no push, no upload. The zip is gitignored. No secrets exist in the two edited files. |
| T-r46-SC | Tampering | supply chain (npm/pip/cargo) | low | accept | N/A — USBee is pure GJS with no package-manager step, and this task installs no packages. Considered and dismissed, not skipped. |
</threat_model>

<verification>
End-to-end, the task is correct when all three hold together:

1. `rtk proxy grep -n '"version' usbee@bitcreed.us/metadata.json` → 13 / 2.8.0 in the repo.
2. `rtk proxy grep -n '^## \[' CHANGELOG.md | head -3` → empty `## [Unreleased]`, then `## [2.8.0] — 2026-09-10`, then `## [2.7.1] — 2026-09-10`.
3. `rtk proxy grep '"version' ~/.local/share/gnome-shell/extensions/usbee@bitcreed.us/metadata.json` → 13 / 2.8.0 on disk.

Plus the two negative conditions: `git tag -l v2.8.0` is empty, and
`git log origin/master..HEAD` shows the release commit is still local.
</verification>

<success_criteria>
- metadata.json: `"version": 13`, `"version-name": "2.8.0"`.
- CHANGELOG.md: dated `## [2.8.0] — 2026-09-10` section holding the prior Unreleased content verbatim; empty `## [Unreleased]` above it; `[2.8.0]:` reference link added; file is 680 lines.
- One commit `chore: release v2.8.0` touching exactly those two files.
- Packed zip verified to contain `src/` entries (actual count recorded).
- Installed on-disk metadata reports 13 / 2.8.0.
- No tag, no push, no Shell restart.
</success_criteria>

<output>
Create `.planning/quick/260910-r46-bump-usbee-version-to-2-8-0-and-deploy-l/260910-r46-SUMMARY.md` when done.

The summary must record, as measured values and not as claims:
the actual zip entry count and uncompressed byte total, the number of
`src/` entries, the final on-disk `grep` output, and an explicit note
that a GNOME Shell restart (Xorg `Alt+F2` → `r`; Wayland re-login) is
still outstanding before 2.8.0 is live.
</output>
